# PLAN-Tools 开发计划 V0.3

> **项目目标**: 从纯前端Vue 3升级到Nuxt 3全栈架构,集成国产AI能力
> **开发周期**: 8周(56天)
> **每日投入**: 建议2-4小时/天
> **当前状态**: Vue 3 纯前端项目已实现基础功能

---

## 📅 开发路线图

```
Week 1-2  │ ████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░ │ 架构迁移 + 基础设施 ✅
Week 3-4  │ ░░░░░░░░░░░░░░░░░░░░░██████████████████░░░░░░ │ AI智能规划 ✅
Week 5-6  │ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░███████ │ AI问答助手 ✅
Week 7-8  │ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░████ │ 测试优化 + 部署 ✅
Week 9+   │ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │ Phase 2: 数据库 + 用户系统
```

---

## 🎯 Phase 1: MVP 开发计划 (Week 1-8)

### 📌 Week 1: Nuxt 3 架构迁移 (5天) ✅ 已完成

**本周目标**: 完成Vue 3到Nuxt 3的迁移,保持所有现有功能正常运行

#### Day 1 (周一): 环境准备 + 项目初始化
**时间估计**: 2-3小时

**今日目标**:
- ✅ 创建Nuxt 3项目骨架
- ✅ 安装核心依赖

**任务清单**:
```bash
# 1. 备份现有项目
git checkout -b backup/vue3-version
git push origin backup/vue3-version

# 2. 创建Nuxt 3项目
cd ..
npx nuxi@latest init plan-tools-nuxt
cd plan-tools-nuxt

# 3. 安装依赖
pnpm add @pinia/nuxt @element-plus/nuxt @nuxtjs/tailwindcss @nuxtjs/i18n
pnpm add dhtmlx-gantt dayjs xlsx
pnpm add -D @types/node

# 4. 验证安装
pnpm dev
# 访问 http://localhost:3000 确认欢迎页面
```

**验收标准**:
- [x] Nuxt 3项目可以正常启动
- [x] 所有依赖安装成功,无报错
- [x] 代码提交到git (commit: "feat: init Nuxt 3 project")

---

#### Day 2 (周二): 拆分main.js + 配置Nuxt模块
**时间估计**: 2-3小时

**今日目标**:
- ✅ 拆分Vue 3的main.js为Nuxt 3的多文件结构
- ✅ 通过Nuxt模块配置Element Plus和Pinia（无需手动插件）

**任务清单**:
```bash
# 1. 创建根组件
touch src/app.vue

# 2. i18n通过 @nuxtjs/i18n 模块管理，无需手动创建插件
# 安装: pnpm add @nuxtjs/i18n
# 在 nuxt.config.ts 的 modules 中添加 '@nuxtjs/i18n' 即可
# 配置见 nuxt.config.ts 中的 i18n 选项

# 3. 配置nuxt.config.ts
# ⭐ 重要：@pinia/nuxt 和 @element-plus/nuxt 作为 modules 注册即可
# 不需要手动创建 plugins/pinia.ts 和 plugins/element-plus.ts
# 否则会导致重复初始化报错
```

**文件清单**:
- [x] `src/app.vue` - 根组件
- [x] `nuxt.config.ts` - Nuxt主配置（含modules、runtimeConfig和i18n配置）
- [x] `src/locales/zh-CN.json` - 中文语言包
- [x] `src/locales/en.json` - 英文语言包

**验收标准**:
- [x] Element Plus通过模块正常加载，组件正常显示
- [x] Pinia通过模块正常加载，Store可正常使用
- [x] @nuxtjs/i18n模块正常加载，国际化切换正常
- [x] 无控制台重复初始化报错
- [x] 代码提交 (commit: "feat: configure Nuxt modules for Element Plus, Pinia and i18n")

---

#### Day 3 (周三): 迁移页面 + 创建路由
**时间估计**: 2-3小时

**今日目标**:
- ✅ 迁移所有视图文件到pages目录
- ✅ 验证路由自动生成

**任务清单**:
```bash
# 1. 复制视图文件到pages
cp ../src/views/ProjectInfoView.vue pages/project-info.vue
cp ../src/views/ProjectPlanView.vue pages/project-plan.vue

# 2. 创建首页
cat > pages/index.vue << 'EOF'
<template>
  <div class="home-container">
    <h1>PLAN-Tools</h1>
    <p>项目计划管理软件</p>
    <div class="actions">
      <el-button type="primary" @click="navigateTo('/project-info')">
        项目信息管理
      </el-button>
      <el-button @click="navigateTo('/project-plan')">
        项目计划管理
      </el-button>
    </div>
  </div>
</template>

<script setup lang="ts">
definePageMeta({
  layout: false
})
</script>

<style scoped>
.home-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
}

.actions {
  margin-top: 2rem;
  display: flex;
  gap: 1rem;
}
</style>
EOF

# 3. 修改视图文件中的路由跳转
# 将 useRouter() 改为 navigateTo()
```

**文件清单**:
- [x] `pages/index.vue` - 首页
- [x] `pages/project-info.vue` - 项目信息页（已升级为 /workspace 的 info tab）
- [x] `pages/project-plan.vue` - 项目计划页（已升级为 /workspace 的 plan tab）

**验收标准**:
- [x] 访问 `/` 显示首页
- [x] 访问 `/project-info` 显示项目信息页（已升级为 /workspace?tab=info）
- [x] 访问 `/project-plan` 显示项目计划页（已升级为 /workspace?tab=plan）
- [x] 代码提交 (commit: "feat: migrate views to pages directory")

---

#### Day 4 (周四): 迁移组件 + 工具函数
**时间估计**: 2-3小时

**今日目标**:
- ✅ 复制所有组件和工具函数
- ✅ 配置TypeScript允许JS文件共存

**任务清单**:
```bash
# 1. 复制组件
cp -r ../src/components/* src/components/

# 2. 复制工具函数
cp -r ../src/utils/* src/utils/

# 3. 复制状态管理
cp -r ../src/store/* src/store/

# 4. 复制国际化文件
cp -r ../src/locales src/locales

# 5. 复制数据文件
cp -r ../src/data src/data

# 6. 复制静态资源
cp -r ../src/assets/* src/assets/

# 7. 配置tsconfig.json允许JS
cat > tsconfig.json << 'EOF'
{
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": false,
    "strict": false,
    "types": ["node"]
  },
  "include": [
    "**/*.js",
    "**/*.ts",
    "**/*.vue"
  ]
}
EOF
```

**验收标准**:
- [x] 所有文件复制完成
- [x] TypeScript配置正确,JS文件无类型错误
- [x] 代码提交 (commit: "feat: copy components and utils from Vue 3 project")

---

#### Day 5 (周五): 测试 + 修复 + 调整
**时间估计**: 2-3小时

**今日目标**:
- ✅ 测试所有页面功能
- ✅ 修复迁移中的问题
- ✅ 确保基本功能正常

**测试清单**:
- [x] 首页可以正常访问
- [x] 项目信息页面可以正常打开
- [x] 项目计划页面可以正常打开
- [x] Element Plus组件正常显示
- [x] 国际化切换正常工作
- [x] localStorage存储正常工作

**常见问题修复**:
```javascript
// 问题1: 路由跳转报错
// 解决: 将 useRouter().push() 改为 navigateTo()

// 问题2: 组件导入报错
// 解决: 使用 Nuxt 自动导入,无需手动 import

// 问题3: Store无法使用
// 解决: 确保 @pinia/nuxt 已在 nuxt.config.ts 的 modules 中注册，Store会自动可用

// 问题4: 国际化不生效
// 解决: 使用 @nuxtjs/i18n 模块，在 nuxt.config.ts 中配置 i18n 选项
// 不要手动创建 plugins/i18n.ts，避免与模块冲突
```

**验收标准**:
- [x] 所有页面正常访问
- [x] 无控制台报错
- [x] 代码提交 (commit: "fix: resolve migration issues")
- [x] 创建里程碑 tag: `git tag v0.3.0-migration`

---

### 📌 Week 2: Server Routes + AI基础设施 (5天) ✅ 已完成

**本周目标**: 搭建Nuxt Server Routes基础设施,为AI集成做准备

#### Day 6 (周一): Server Routes入门
**时间估计**: 2-3小时

**今日目标**:
- ✅ 创建第一个Server API
- ✅ 测试API调用

**任务清单**:
```bash
# 1. 创建API目录
mkdir -p server/api

# 2. 创建健康检查接口
cat > server/api/health.get.ts << 'EOF'
export default defineEventHandler((event) => {
  return {
    status: 'ok',
    timestamp: new Date().toISOString(),
    version: '0.3.0'
  }
})
EOF

# 3. 创建Hello World接口
cat > server/api/hello.post.ts << 'EOF'
export default defineEventHandler(async (event) => {
  const body = await readBody(event)
  return {
    message: `Hello, ${body.name}!`,
    timestamp: new Date().toISOString()
  }
})
EOF

# 4. 测试API
curl http://localhost:3000/api/health
curl -X POST http://localhost:3000/api/hello -H "Content-Type: application/json" -d '{"name":"World"}'
```

**验收标准**:
- [x] `/api/health` 返回正常
- [x] `/api/hello` POST请求正常
- [x] 代码提交 (commit: "feat: add Server Routes - health check + hello API")

---

#### Day 7 (周二): AI服务封装 - 统一客户端
**时间估计**: 3-4小时

**今日目标**:
- ✅ 创建统一AI客户端（DeepSeek + QWen共用OpenAI兼容格式）
- ✅ 实现AI对话接口
- ✅ 配置runtimeConfig管理API Key

**任务清单**:
```bash
# 1. 创建AI工具目录
mkdir -p server/utils/ai

# 2. 创建AI类型定义
cat > server/utils/ai/types.ts << 'EOF'
interface Message {
  role: 'system' | 'user' | 'assistant'
  content: string
}

interface AIResponse {
  content: string
  model: string
  provider: string
  usage: {
    promptTokens: number
    completionTokens: number
    totalTokens: number
  }
}
EOF

# 3. 创建统一AI客户端
cat > server/utils/ai/client.ts << 'EOF'
export async function callAI(
  messages: Message[],
  options?: { provider?: 'deepseek' | 'qwen'; model?: string; temperature?: number }
): Promise<AIResponse> {
  const config = useRuntimeConfig()

  const provider = options?.provider || 'deepseek'

  const providerConfig = {
    deepseek: {
      baseURL: config.deepseekApiBase || 'https://api.deepseek.com',
      apiKey: config.deepseekApiKey,
      model: options?.model || config.deepseekModel || 'deepseek-chat'
    },
    qwen: {
      baseURL: config.qwenApiBase || 'https://dashscope.aliyuncs.com/compatible-mode/v1',
      apiKey: config.qwenApiKey,
      model: options?.model || config.qwenModel || 'qwen-max'
    }
  }

  const { baseURL, apiKey, model } = providerConfig[provider]

  if (!apiKey) {
    throw createError({
      statusCode: 500,
      statusMessage: `${provider} API Key not configured. Please set it in .env file.`
    })
  }

  try {
    const response = await $fetch('/chat/completions', {
      baseURL,
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${apiKey}`,
        'Content-Type': 'application/json'
      },
      body: {
        model,
        messages,
        temperature: options?.temperature ?? 0.7,
        max_tokens: 2000
      }
    })

    return {
      content: response.choices[0]?.message?.content || '',
      model,
      provider,
      usage: {
        promptTokens: response.usage?.prompt_tokens || 0,
        completionTokens: response.usage?.completion_tokens || 0,
        totalTokens: response.usage?.total_tokens || 0
      }
    }
  } catch (error: any) {
    console.error(`AI API error (${provider}):`, error?.message || error)
    throw createError({
      statusCode: error?.statusCode || 500,
      statusMessage: `Failed to call ${provider} API: ${error?.message || 'Unknown error'}`
    })
  }
}
EOF

# 4. 创建AI对话接口
cat > server/api/ai/chat.post.ts << 'EOF'
import { callAI } from '~/server/utils/ai/client'

export default defineEventHandler(async (event) => {
  const body = await readBody(event)
  const { messages } = body

  if (!messages || !Array.isArray(messages)) {
    throw createError({
      statusCode: 400,
      statusMessage: 'Invalid messages format'
    })
  }

  const response = await callAI(messages, { provider: 'qwen', model: 'qwen-turbo' })

  return response
})
EOF

# 5. 创建AI状态检测接口
cat > server/api/ai/status.get.ts << 'EOF'
export default defineEventHandler((event) => {
  const config = useRuntimeConfig()
  return {
    available: !!(config.deepseekApiKey || config.qwenApiKey),
    providers: [
      { name: 'deepseek', configured: !!config.deepseekApiKey },
      { name: 'qwen', configured: !!config.qwenApiKey }
    ]
  }
})
EOF

# 6. 配置环境变量
cat >> .env << 'EOF'
# DeepSeek配置
DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_API_BASE=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# 阿里QWen配置（OpenAI兼容模式）
QWEN_API_KEY=your_api_key_here
QWEN_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-max
EOF
```

> ⭐ **重要**：DeepSeek和QWen均支持OpenAI兼容格式（`/v1/chat/completions`），因此使用统一客户端，只需切换`baseURL`和`apiKey`。无需为每个服务商创建单独的Provider类。

**验收标准**:
- [x] 统一AI客户端创建成功
- [x] `/api/ai/chat` 接口可以调用
- [x] `/api/ai/status` 接口返回AI可用性
- [x] API Key通过`useRuntimeConfig()`读取，不使用`process.env`
- [x] 代码提交 (commit: "feat: add unified AI client with runtimeConfig")

---

#### Day 8 (周三): AI模型路由 + 重试机制
**时间估计**: 2-3小时

**今日目标**:
- ✅ 创建AI模型路由（按任务类型选择模型）
- ✅ 实现重试和降级机制
- ✅ 测试DeepSeek和QWen切换

**任务清单**:
```bash
# 1. 创建AI模型路由
cat > server/utils/ai/router.ts << 'EOF'
type AITaskType = 'task-breakdown' | 'duration-estimation' | 'chat' | 'health-diagnosis' | 'report-generation'

interface AIProviderConfig {
  provider: 'deepseek' | 'qwen'
  model: string
}

export function selectAIProvider(taskType: AITaskType): AIProviderConfig {
  const modelConfig: Record<AITaskType, AIProviderConfig> = {
    'task-breakdown': { provider: 'deepseek', model: 'deepseek-chat' },
    'duration-estimation': { provider: 'qwen', model: 'qwen-max' },
    'chat': { provider: 'qwen', model: 'qwen-turbo' },
    'health-diagnosis': { provider: 'deepseek', model: 'deepseek-chat' },
    'report-generation': { provider: 'qwen', model: 'qwen-plus' }
  }

  return modelConfig[taskType]
}
EOF

# 2. 创建重试机制
cat > server/utils/ai/retry.ts << 'EOF'
import { callAI } from './client'

export async function callAIWithRetry(
  messages: Message[],
  options?: { provider?: 'deepseek' | 'qwen'; model?: string; temperature?: number },
  maxRetries = 3
): Promise<AIResponse> {
  for (let i = 0; i < maxRetries; i++) {
    try {
      return await callAI(messages, options)
    } catch (error: any) {
      if (i === maxRetries - 1) throw error

      if (error?.statusCode === 429) {
        await new Promise(resolve => setTimeout(resolve, 1000 * (i + 1)))
      } else if (error?.statusCode >= 500) {
        continue
      } else {
        throw error
      }
    }
  }
  throw new Error('Max retries exceeded')
}
EOF

# 3. 创建AI降级检测
cat > server/utils/ai/fallback.ts << 'EOF'
export function isAIAvailable(): boolean {
  const config = useRuntimeConfig()
  return !!(config.deepseekApiKey || config.qwenApiKey)
}
EOF
```

> ⭐ **架构说明**：Day 7已创建统一客户端`callAI()`，Day 8不再需要单独的QWen客户端。QWen使用OpenAI兼容格式（`https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions`），与DeepSeek共用同一套代码，只需切换`baseURL`和`apiKey`。

**验收标准**:
- [x] 模型路由逻辑正确
- [x] 重试机制在429/500错误时正常工作
- [x] DeepSeek和QWen均可正常调用
- [x] 代码提交 (commit: "feat: add AI model router, retry mechanism and fallback")

---

#### Day 9 (周四): AI可用性检测 + 前端composable
**时间估计**: 2-3小时

**今日目标**:
- ✅ 创建前端AI可用性检测composable
- ✅ 实现AI配置信息接口（不暴露Key）
- ✅ 理解MVP阶段API Key服务端管理方案

**任务清单**:
```typescript
// 1. 创建前端AI可用性composable
// src/composables/useAI.ts
export function useAIAvailability() {
  const aiAvailable = ref(false)
  const providers = ref<Array<{ name: string; configured: boolean }>>([])

  onMounted(async () => {
    try {
      const data = await $fetch('/api/ai/status')
      aiAvailable.value = data.available
      providers.value = data.providers
    } catch {
      aiAvailable.value = false
    }
  })

  return { aiAvailable, providers }
}

// 2. AI配置信息接口（仅返回可用性，不暴露Key）
// server/api/ai/config.get.ts
export default defineEventHandler((event) => {
  const config = useRuntimeConfig()
  return {
    providers: [
      {
        name: 'deepseek',
        configured: !!config.deepseekApiKey,
        models: ['deepseek-chat', 'deepseek-coder']
      },
      {
        name: 'qwen',
        configured: !!config.qwenApiKey,
        models: ['qwen-max', 'qwen-plus', 'qwen-turbo']
      }
    ]
  }
})
```

> ⭐ **MVP阶段API Key安全方案**：
> - API Key配置在服务端`.env`文件中，通过`nuxt.config.ts`的`runtimeConfig`暴露给Server Routes
> - 前端**不存储**API Key，不创建`useAIStore`来管理Key
> - 前端只检测AI是否可用（`/api/ai/status`），然后直接调用`/api/ai/*`接口
> - 如需切换Key，修改服务端`.env`后重启应用
> - Phase 2引入用户系统后，再实现用户级API Key管理

**验收标准**:
- [x] AI可用性检测composable正常工作
- [x] `/api/ai/status` 接口返回正确
- [x] `/api/ai/config` 接口不暴露API Key
- [x] 代码提交 (commit: "feat: add AI availability composable and config API")

---

#### Day 10 (周五): AI状态提示UI组件
**时间估计**: 3-4小时

**今日目标**:
- ✅ 创建AI状态提示组件（显示AI是否可用）
- ✅ 创建AI未配置时的引导提示

**任务清单**:
```vue
<!-- 1. 创建AI状态提示组件 -->
<!-- src/components/AIAssistant/AIStatusBanner.vue -->
<template>
  <div v-if="!aiAvailable" class="ai-status-banner">
    <el-alert
      title="AI服务未配置"
      type="warning"
      :closable="false"
      show-icon
    >
      <template #default>
        <p>AI功能需要配置API Key才能使用。请在服务端`.env`文件中配置：</p>
        <el-code>
          DEEPSEEK_API_KEY=sk-xxx<br>
          QWEN_API_KEY=sk-xxx
        </el-code>
        <p class="mt-2">
          配置后重启应用即可使用AI功能。
          <el-button type="primary" link @click="openDoc">
            查看配置文档
          </el-button>
        </p>
      </template>
    </el-alert>
  </div>
</template>

<script setup lang="ts">
import { useAIAvailability } from '~/composables/useAI'

const { aiAvailable } = useAIAvailability()

const openDoc = () => {
  window.open('https://github.com/your-org/plan-tools#ai-configuration', '_blank')
}
</script>
```

> ⭐ **MVP简化**：Phase 1中AI配置通过服务端`.env`管理，前端无需API Key输入表单。Phase 2引入用户系统后，再实现用户级API Key配置UI。

**验收标准**:
- [x] AI状态提示组件正常显示
- [x] AI未配置时显示引导信息
- [x] AI已配置时隐藏提示
- [x] 代码提交 (commit: "feat: add AI status banner component with i18n")

---

### 📌 Week 3-4: AI智能规划功能 (10天) ✅ 已完成

**本周目标**: 实现AI自动生成项目任务计划的核心功能

#### Day 11-12 (周一-周二): 行业模板库
**时间估计**: 5-6小时 (2天)

**今日目标**:
- ✅ 创建行业模板数据结构
- ✅ 实现3个内置模板
- ✅ 创建模板选择器组件

**任务清单**:
```typescript
// 1. 创建模板数据结构
// src/data/templates.ts
export const industryTemplates = [
  {
    id: 'software-dev',
    name: '软件开发项目',
    icon: '💻',
    description: '适用于网站、APP、系统开发等项目',
    phases: [
      {
        name: '需求分析',
        tasks: [
          { name: '需求调研', duration: 3, deliverable: '需求调研报告' },
          { name: '需求分析', duration: 5, deliverable: '需求规格说明书' },
          { name: '原型设计', duration: 4, deliverable: '原型图' }
        ]
      },
      {
        name: '架构设计',
        tasks: [
          { name: '技术方案设计', duration: 3, deliverable: '技术方案文档' },
          { name: '数据库设计', duration: 2, deliverable: '数据库设计文档' },
          { name: 'API接口设计', duration: 3, deliverable: 'API接口文档' }
        ]
      },
      {
        name: '开发阶段',
        tasks: [
          { name: '前端开发', duration: 15, deliverable: '前端代码' },
          { name: '后端开发', duration: 15, deliverable: '后端代码' },
          { name: '接口联调', duration: 5, deliverable: '联调报告' }
        ]
      },
      {
        name: '测试阶段',
        tasks: [
          { name: '单元测试', duration: 5, deliverable: '测试用例' },
          { name: '集成测试', duration: 5, deliverable: '测试报告' },
          { name: '用户验收测试', duration: 3, deliverable: '验收报告' }
        ]
      },
      {
        name: '部署上线',
        tasks: [
          { name: '部署准备', duration: 2, deliverable: '部署清单' },
          { name: '正式部署', duration: 1, deliverable: '生产环境' },
          { name: '上线监控', duration: 3, deliverable: '监控报告' }
        ]
      }
    ]
  },
  {
    id: 'marketing',
    name: '市场营销活动',
    icon: '📢',
    description: '适用于市场推广、品牌活动等',
    phases: [
      {
        name: '市场调研',
        tasks: [
          { name: '目标市场分析', duration: 3, deliverable: '市场分析报告' },
          { name: '竞品分析', duration: 2, deliverable: '竞品分析报告' },
          { name: '用户画像研究', duration: 3, deliverable: '用户画像' }
        ]
      },
      {
        name: '策划阶段',
        tasks: [
          { name: '活动策划', duration: 5, deliverable: '活动策划案' },
          { name: '预算制定', duration: 2, deliverable: '预算表' },
          { name: '资源协调', duration: 3, deliverable: '资源清单' }
        ]
      },
      {
        name: '执行阶段',
        tasks: [
          { name: '物料准备', duration: 5, deliverable: '宣传物料' },
          { name: '渠道推广', duration: 10, deliverable: '推广数据' },
          { name: '活动执行', duration: 7, deliverable: '活动执行报告' }
        ]
      },
      {
        name: '复盘阶段',
        tasks: [
          { name: '数据收集分析', duration: 3, deliverable: '数据分析报告' },
          { name: '效果评估', duration: 2, deliverable: '效果评估报告' },
          { name: '总结复盘', duration: 2, deliverable: '复盘报告' }
        ]
      }
    ]
  },
  {
    id: 'construction',
    name: '建筑工程项目',
    icon: '🏗️',
    description: '适用于建筑施工、装修等工程项目',
    phases: [
      {
        name: '前期准备',
        tasks: [
          { name: '项目立项', duration: 5, deliverable: '立项批复' },
          { name: '设计图纸', duration: 10, deliverable: '设计图纸' },
          { name: '招投标', duration: 7, deliverable: '中标通知书' }
        ]
      },
      {
        name: '基础工程',
        tasks: [
          { name: '场地平整', duration: 5, deliverable: '场地验收' },
          { name: '基础施工', duration: 15, deliverable: '基础验收' },
          { name: '主体结构', duration: 20, deliverable: '主体验收' }
        ]
      },
      {
        name: '装修工程',
        tasks: [
          { name: '水电安装', duration: 10, deliverable: '水电验收' },
          { name: '装修施工', duration: 15, deliverable: '装修验收' },
          { name: '设备安装', duration: 7, deliverable: '设备调试' }
        ]
      },
      {
        name: '验收交付',
        tasks: [
          { name: '竣工验收', duration: 3, deliverable: '竣工验收报告' },
          { name: '资料归档', duration: 3, deliverable: '档案资料' },
          { name: '交付使用', duration: 1, deliverable: '交付确认书' }
        ]
      }
    ]
  }
]

// 2. 创建模板选择器组件
// src/components/AIAssistant/AITemplateSelector.vue
<template>
  <div class="template-selector">
    <div class="template-grid">
      <div
        v-for="template in templates"
        :key="template.id"
        class="template-card"
        :class="{ active: selectedId === template.id }"
        @click="handleSelect(template)"
      >
        <div class="template-icon">{{ template.icon }}</div>
        <div class="template-name">{{ template.name }}</div>
        <div class="template-desc">{{ template.description }}</div>
        <div class="template-info">
          <span>{{ template.phases.length }} 个阶段</span>
          <span>{{ getTotalTasks(template) }} 个任务</span>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { industryTemplates } from '~/data/templates'

const templates = industryTemplates
const selectedId = ref<string>()

const emit = defineEmits<{
  select: [template: typeof templates[0]]
}>()

const getTotalTasks = (template: typeof templates[0]) => {
  return template.phases.reduce((sum, phase) => {
    return sum + phase.tasks.length
  }, 0)
}

const handleSelect = (template: typeof templates[0]) => {
  selectedId.value = template.id
  emit('select', template)
}
</script>
```

**验收标准**:
- [x] 3个行业模板创建完成
- [x] 模板选择器UI正常显示
- [x] 可以点击选择模板
- [x] 代码提交 (commit: "feat: add industry template library with selector component")

---

#### Day 13-15 (周三-周五): AI任务生成接口
**时间估计**: 9-12小时 (3天)

**今日目标**:
- ✅ 实现WBS生成接口
- ✅ 实现工期估算接口
- ✅ 实现依赖推荐接口
- ✅ 测试AI生成功能

**任务清单**:
```typescript
// 1. WBS生成接口
// server/api/ai/wbs.post.ts
import { callAI } from '~/server/utils/ai/client'

export default defineEventHandler(async (event) => {
  const body = await readBody(event)
  const { projectDescription, industry, requirements, template } = body

  // 构建Prompt
  const systemPrompt = `你是一个专业的项目经理,擅长制定项目计划。请根据用户的项目描述生成详细的任务分解结构(WBS)。

要求:
1. 任务分解要符合SMART原则(具体、可衡量、可达成、相关性、时限性)
2. 每个任务包含:任务名称、工期(天)、交付物、描述
3. 任务之间要有合理的依赖关系
4. 返回JSON格式,嵌套结构表示父子任务

返回格式示例:
{
  "tasks": [
    {
      "name": "需求分析",
      "duration": 5,
      "deliverable": "需求规格说明书",
      "description": "收集和分析项目需求",
      "children": [
        {
          "name": "需求调研",
          "duration": 3,
          "deliverable": "需求调研报告",
          "description": "与 stakeholders 进行需求调研"
        }
      ]
    }
  ]
}`

  const userPrompt = `项目描述: ${projectDescription}
行业类型: ${industry}
特殊要求: ${requirements || '无'}
${template ? `参考模板: ${template.name}` : ''}

请生成任务分解结构,返回JSON格式。`

  const messages = [
    { role: 'system', content: systemPrompt },
    { role: 'user', content: userPrompt }
  ]

  try {
    const response = await callAI(messages, { provider: 'deepseek' })
    const content = response.content

    let tasks
    try {
      tasks = parseAIJsonResponse(content, {
        requiredFields: ['tasks'],
        tasks: { type: 'array', required: true }
      })
    } catch (parseError: any) {
      throw createError({
        statusCode: 500,
        statusMessage: `AI返回格式错误: ${parseError.message}，请重试`
      })
    }

    if (!tasks.tasks || !Array.isArray(tasks.tasks) || tasks.tasks.length === 0) {
      throw createError({
        statusCode: 500,
        statusMessage: 'AI返回的任务列表为空，请提供更详细的项目描述'
      })
    }

    // 计算统计信息
    const totalTasks = countTasks(tasks.tasks)
    const estimatedDuration = calculateTotalDuration(tasks.tasks)
    const criticalPathCount = findCriticalPath(tasks.tasks).length

    return {
      tasks: tasks.tasks,
      statistics: {
        totalTasks,
        estimatedDuration,
        criticalPathCount
      }
    }
  } catch (error) {
    console.error('WBS generation error:', error)
    throw error
  }
})

// 辅助函数
function countTasks(tasks: any[]): number {
  let count = 0
  for (const task of tasks) {
    count++
    if (task.children) {
      count += countTasks(task.children)
    }
  }
  return count
}

function calculateTotalDuration(tasks: any[]): number {
  // 简化计算:返回最长路径的总工期
  return tasks.reduce((max, task) => {
    const duration = task.duration || 0
    const childrenDuration = task.children
      ? calculateTotalDuration(task.children)
      : 0
    return Math.max(max, duration + childrenDuration)
  }, 0)
}

function findCriticalPath(tasks: any[]): any[] {
  // 简化实现:返回所有有依赖的任务
  return tasks.filter(task => task.dependencies && task.dependencies.length > 0)
}

// 2. 工期估算接口
// server/api/ai/estimate-duration.post.ts
import { callAI } from '~/server/utils/ai/client'

export default defineEventHandler(async (event) => {
  const body = await readBody(event)
  const { taskDescription, complexity, teamSkills } = body

  const systemPrompt = `你是一个项目管理专家,擅长估算任务工期。请使用PERT(计划评审技术)方法估算任务工期。

PERT公式:
- 乐观工期(O): 最快完成时间
- 最可能工期(M): 正常情况下完成时间
- 悲观工期(P): 最慢完成时间
- 期望工期 = (O + 4M + P) / 6

请根据任务描述、复杂度(1-5级)和团队能力,返回三种工期估算。`

  const userPrompt = `任务描述: ${taskDescription}
复杂度: ${complexity}/5
团队技能: ${teamSkills?.join(', ') || '未知'}

请估算工期,返回JSON格式:
{
  "optimistic": 3,
  "likely": 5,
  "pessimistic": 8,
  "expected": 5.17,
  "confidence": 0.85
}`

  const messages = [
    { role: 'system', content: systemPrompt },
    { role: 'user', content: userPrompt }
  ]

  try {
    const response = await callAI(messages, { provider: 'qwen' })
    const result = parseAIJsonResponse(response.content, {
      optimistic: { type: 'number', required: true },
      likely: { type: 'number', required: true },
      pessimistic: { type: 'number', required: true },
      expected: { type: 'number', required: true }
    })

    return result
  } catch (error) {
    console.error('Duration estimation error:', error)
    throw error
  }
})

// 3. 依赖关系推荐接口
// server/api/ai/suggest-dependencies.post.ts
import { callAI } from '~/server/utils/ai/client'

export default defineEventHandler(async (event) => {
  const body = await readBody(event)
  const { tasks } = body

  const systemPrompt = `你是一个项目管理专家,擅长分析任务之间的依赖关系。

请根据任务列表,分析并推荐任务之间的依赖关系:
1. 识别逻辑上的前置任务(如:设计必须在开发之前)
2. 避免循环依赖
3. 标注关键路径任务

返回JSON格式:
{
  "dependencies": [
    {
      "taskId": "2",
      "dependsOn": ["1"],
      "reason": "UI设计需要基于需求分析的结果"
    }
  ]
}`

  const taskList = tasks.map((t: any, i: number) =>
    `${i + 1}. ${t.name} (${t.duration || 0}天)`
  ).join('\n')

  const userPrompt = `任务列表:\n${taskList}\n\n请推荐依赖关系。`

  const messages = [
    { role: 'system', content: systemPrompt },
    { role: 'user', content: userPrompt }
  ]

  try {
    const response = await callAI(messages, { provider: 'deepseek' })
    const result = parseAIJsonResponse(response.content, {
      dependencies: { type: 'array', required: true }
    })

    return result
  } catch (error) {
    console.error('Dependency suggestion error:', error)
    throw error
  }
})
```

**验收标准**:
- [x] WBS生成接口可以正常调用
- [x] 返回的任务结构正确
- [x] 工期估算接口返回合理数据
- [x] 依赖推荐接口正常工作
- [x] 代码提交 (commit: "feat: add AI task generation APIs - WBS, duration estimation, dependency suggestion")

---

#### Day 16-18 (周一-周三): AI规划向导UI
**时间估计**: 9-12小时 (3天)

**今日目标**:
- ✅ 创建AI项目规划向导组件
- ✅ 实现多步骤表单
- ✅ 任务预览和编辑
- ✅ 批量导入功能

**任务清单**:
```vue
<!-- AI项目规划向导 -->
<!-- src/components/AIAssistant/AIProjectWizard.vue -->
<template>
  <el-dialog
    v-model="dialogVisible"
    title="AI智能项目规划"
    width="900px"
    :close-on-click-modal="false"
  >
    <!-- 步骤指示器 -->
    <div class="wizard-steps">
      <div
        v-for="(step, index) in steps"
        :key="index"
        class="step-item"
        :class="{
          active: currentStep === index,
          completed: currentStep > index
        }"
      >
        <div class="step-number">{{ index + 1 }}</div>
        <div class="step-title">{{ step.title }}</div>
      </div>
    </div>

    <!-- 步骤内容 -->
    <div class="wizard-content">
      <!-- Step 1: 项目描述 -->
      <div v-show="currentStep === 0" class="step-panel">
        <h3>描述您的项目</h3>
        <el-form :model="form" label-width="100px">
          <el-form-item label="项目名称">
            <el-input
              v-model="form.projectName"
              placeholder="例如:开发一个电商网站"
            />
          </el-form-item>

          <el-form-item label="项目描述">
            <el-input
              v-model="form.projectDescription"
              type="textarea"
              :rows="4"
              placeholder="请详细描述您的项目目标、范围、主要功能等..."
            />
          </el-form-item>

          <el-form-item label="行业类型">
            <el-select v-model="form.industry" placeholder="选择行业">
              <el-option label="软件开发" value="software" />
              <el-option label="市场营销" value="marketing" />
              <el-option label="建筑工程" value="construction" />
              <el-option label="其他" value="other" />
            </el-select>
          </el-form-item>

          <el-form-item label="特殊要求">
            <el-input
              v-model="form.requirements"
              type="textarea"
              :rows="2"
              placeholder="例如:需要支持移动端、需要符合GDPR规范等..."
            />
          </el-form-item>
        </el-form>
      </div>

      <!-- Step 2: 选择模板 -->
      <div v-show="currentStep === 1" class="step-panel">
        <h3>选择行业模板 (可选)</h3>
        <AITemplateSelector
          v-model="form.selectedTemplate"
          @select="handleTemplateSelect"
        />

        <el-divider>或</el-divider>

        <el-button @click="skipTemplate">
          跳过,由AI完全自定义生成
        </el-button>
      </div>

      <!-- Step 3: AI生成中 -->
      <div v-show="currentStep === 2" class="step-panel">
        <div class="generating-state">
          <el-icon class="is-loading" :size="60">
            <Loading />
          </el-icon>
          <h3>AI正在生成项目计划...</h3>
          <p>{{ generatingStatus }}</p>
          <el-progress
            :percentage="generatingProgress"
            :status="generatingProgress === 100 ? 'success' : undefined"
          />
        </div>
      </div>

      <!-- Step 4: 任务预览 -->
      <div v-show="currentStep === 3" class="step-panel">
        <h3>任务预览</h3>

        <div class="statistics">
          <el-statistic title="任务总数" :value="statistics.totalTasks" />
          <el-statistic title="预计工期" :value="statistics.estimatedDuration" suffix="天" />
          <el-statistic title="关键路径" :value="statistics.criticalPathCount" suffix="个任务" />
        </div>

        <el-divider />

        <div class="task-tree">
          <TaskTree
            :tasks="generatedTasks"
            @edit="handleEditTask"
          />
        </div>
      </div>
    </div>

    <!-- 底部按钮 -->
    <template #footer>
      <div class="wizard-footer">
        <el-button
          v-if="currentStep > 0 && currentStep < 3"
          @click="previousStep"
        >
          上一步
        </el-button>

        <el-button
          v-if="currentStep < 2"
          type="primary"
          @click="nextStep"
          :disabled="!canNextStep"
        >
          下一步
        </el-button>

        <el-button
          v-if="currentStep === 3"
          @click="dialogVisible = false"
        >
          取消
        </el-button>

        <el-button
          v-if="currentStep === 3"
          type="primary"
          @click="handleImport"
          :loading="importing"
        >
          导入到项目
        </el-button>
      </div>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { useTasksStore } from '~/store/tasks'
import { ElMessage } from 'element-plus'
import AITemplateSelector from './AITemplateSelector.vue'
import TaskTree from '../TaskTree.vue'

const dialogVisible = defineModel<boolean>()
const tasksStore = useTasksStore()

const currentStep = ref(0)
const importing = ref(false)
const generatingProgress = ref(0)
const generatingStatus = ref('')

const steps = [
  { title: '项目描述' },
  { title: '选择模板' },
  { title: 'AI生成' },
  { title: '任务预览' }
]

const form = ref({
  projectName: '',
  projectDescription: '',
  industry: '',
  requirements: '',
  selectedTemplate: null
})

const generatedTasks = ref([])
const statistics = ref({
  totalTasks: 0,
  estimatedDuration: 0,
  criticalPathCount: 0
})

const canNextStep = computed(() => {
  switch (currentStep.value) {
    case 0:
      return form.value.projectName && form.value.projectDescription
    case 1:
      return true // 模板可选
    default:
      return true
  }
})

const nextStep = async () => {
  if (currentStep.value === 1) {
    // 开始生成任务
    await generateTasks()
  }
  currentStep.value++
}

const previousStep = () => {
  if (currentStep.value > 0) {
    currentStep.value--
  }
}

const handleTemplateSelect = (template: any) => {
  form.value.selectedTemplate = template
}

const skipTemplate = () => {
  form.value.selectedTemplate = null
  nextStep()
}

const generateTasks = async () => {
  generatingProgress.value = 0
  generatingStatus.value = '正在调用AI分析项目...'

  try {
    generatingProgress.value = 20
    generatingStatus.value = 'AI正在分解任务...'

    const response = await $fetch('/api/ai/wbs', {
      method: 'POST',
      body: {
        projectDescription: form.value.projectDescription,
        industry: form.value.industry,
        requirements: form.value.requirements,
        template: form.value.selectedTemplate
      }
    })

    generatingProgress.value = 80
    generatingStatus.value = '正在计算工期和依赖关系...'

    generatedTasks.value = response.tasks
    statistics.value = response.statistics

    generatingProgress.value = 100
    generatingStatus.value = '生成完成!'
  } catch (error) {
    console.error('Task generation error:', error)
    ElMessage.error('任务生成失败,请重试')
    currentStep.value = 1 // 返回模板选择页
  }
}

const handleEditTask = (task: any) => {
  // TODO: 实现任务编辑
  console.log('Edit task:', task)
}

const handleImport = async () => {
  importing.value = true

  try {
    // 批量导入任务
    for (const task of generatedTasks.value) {
      await tasksStore.addTask(task)
    }

    ElMessage.success(`成功导入 ${statistics.value.totalTasks} 个任务`)
    dialogVisible.value = false
  } catch (error) {
    console.error('Task import error:', error)
    ElMessage.error('任务导入失败')
  } finally {
    importing.value = false
  }
}
</script>

<style scoped>
.wizard-steps {
  display: flex;
  justify-content: space-between;
  margin-bottom: 2rem;
  padding: 0 2rem;
}

.step-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
  opacity: 0.5;
}

.step-item.active {
  opacity: 1;
}

.step-item.completed {
  opacity: 1;
  color: #67c23a;
}

.step-number {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: 2px solid currentColor;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
}

.step-item.active .step-number {
  background: #409eff;
  color: white;
  border-color: #409eff;
}

.step-item.completed .step-number {
  background: #67c23a;
  color: white;
  border-color: #67c23a;
}

.wizard-content {
  min-height: 400px;
  padding: 0 2rem;
}

.step-panel h3 {
  margin-bottom: 1.5rem;
  color: #303133;
}

.generating-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 300px;
  gap: 1.5rem;
}

.statistics {
  display: flex;
  gap: 2rem;
  margin-bottom: 1rem;
}

.task-tree {
  max-height: 400px;
  overflow-y: auto;
}

.wizard-footer {
  display: flex;
  justify-content: flex-end;
  gap: 1rem;
}
</style>
```

**验收标准**:
- [x] 向导对话框正常显示
- [x] 步骤切换流畅
- [x] AI生成任务正常
- [x] 任务预览正确显示
- [x] 批量导入功能正常
- [x] 代码提交 (commit: "feat: add AI project wizard UI with task tree preview and batch import")

---

#### Day 19-20 (周四-周五): 测试和优化
**时间估计**: 5-6小时 (2天)

**今日目标**:
- ✅ 端到端测试AI规划功能
- ✅ 优化用户体验
- ✅ 处理边界情况
- ✅ 文档完善

**测试清单**:
```bash
# 测试场景
✅ 场景1: 无模板,完全AI生成
✅ 场景2: 选择软件模板,基于模板生成
✅ 场景3: 项目描述为空时的提示
✅ 场景4: AI生成失败时的错误处理
✅ 场景5: 重复生成的处理
✅ 场景6: 大批量任务的导入性能
✅ 场景7: 生成的任务可以正常编辑
✅ 场景8: WBS编号自动生成
```

**优化项**:
- [ ] 添加加载骨架屏
- [ ] 优化错误提示信息
- [ ] 添加生成进度条
- [ ] 优化任务预览性能
- [ ] 添加撤销/重做功能

**验收标准**:
- [x] 所有测试场景通过
- [x] 无明显性能问题
- [x] 用户体验流畅
- [x] 代码提交 (commit: "test: optimize AI planning - add 503 error handling and progress UX")
- [x] 创建tag: `git tag v0.3.1-ai-planning`

---

### 📌 Week 5-6: AI问答助手功能 (10天) ✅ 已完成

**Week 5目标**: 实现AI对话式助手

#### Day 21-22 (周一-周二): 对话界面
**时间估计**: 5-6小时 (2天)

**今日目标**:
- ✅ 创建浮动按钮
- ✅ 创建聊天对话框
- ✅ 实现消息列表
- ✅ 实现输入框和发送

**核心代码**:
```vue
<!-- AI浮动按钮 -->
<!-- src/components/AIAssistant/AIFloatingButton.vue -->
<template>
  <div class="ai-float-button" @click="openChat">
    <el-icon :size="24"><ChatDotRound /></el-icon>
    <div v-if="hasUnread" class="unread-badge"></div>
    <div class="tooltip">AI助手</div>
  </div>
</template>

<script setup lang="ts">
const emit = defineEmits<{
  open: []
}>()

const hasUnread = ref(false)

const openChat = () => {
  emit('open')
}
</script>

<style scoped>
.ai-float-button {
  position: fixed;
  bottom: 2rem;
  right: 2rem;
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
  transition: all 0.3s;
  z-index: 1000;
}

.ai-float-button:hover {
  transform: scale(1.1);
  box-shadow: 0 6px 16px rgba(102, 126, 234, 0.6);
}

.unread-badge {
  position: absolute;
  top: 4px;
  right: 4px;
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: #f56c6c;
  border: 2px solid white;
}

.tooltip {
  position: absolute;
  right: 60px;
  background: rgba(0, 0, 0, 0.8);
  color: white;
  padding: 4px 12px;
  border-radius: 4px;
  font-size: 14px;
  white-space: nowrap;
  opacity: 0;
  transition: opacity 0.3s;
  pointer-events: none;
}

.ai-float-button:hover .tooltip {
  opacity: 1;
}
</style>

<!-- AI聊天对话框 -->
<!-- src/components/AIAssistant/AIChatDialog.vue -->
<template>
  <div
    v-show="visible"
    class="ai-chat-dialog"
  >
    <!-- 头部 -->
    <div class="chat-header">
      <div class="header-title">
        <el-icon><ChatDotRound /></el-icon>
        <span>AI助手</span>
      </div>
      <div class="header-actions">
        <el-button text @click="clearHistory">
          <el-icon><Delete /></el-icon>
        </el-button>
        <el-button text @click="visible = false">
          <el-icon><Close /></el-icon>
        </el-button>
      </div>
    </div>

    <!-- 消息列表 -->
    <div ref="messagesRef" class="chat-messages">
      <!-- 欢迎消息 -->
      <div v-if="messages.length === 0" class="welcome-message">
        <div class="welcome-avatar">
          <el-icon :size="40"><ChatDotRound /></el-icon>
        </div>
        <div class="welcome-content">
          <h4>你好!我是PLAN-Tools AI助手</h4>
          <p>我可以帮你:</p>
          <ul>
            <li>📊 查询项目状态</li>
            <li>📅 分析任务进度</li>
            <li>⚠️ 预警延期风险</li>
            <li>📝 生成项目报告</li>
            <li>🔧 执行项目操作</li>
          </ul>
          <p>试试问我:</p>
          <div class="quick-questions">
            <el-tag
              v-for="question in quickQuestions"
              :key="question"
              @click="sendQuickQuestion(question)"
            >
              {{ question }}
            </el-tag>
          </div>
        </div>
      </div>

      <!-- 消息列表 -->
      <div
        v-for="(message, index) in messages"
        :key="index"
        class="message-item"
        :class="message.role"
      >
        <div class="message-avatar">
          <el-icon v-if="message.role === 'user'"><User /></el-icon>
          <el-icon v-else><ChatDotRound /></el-icon>
        </div>
        <div class="message-content">
          <div class="message-text" v-html="formatMessage(message.content)"></div>
          <div class="message-time">
            {{ formatTime(message.timestamp) }}
          </div>
        </div>
      </div>

      <!-- 输入中提示 -->
      <div v-if="isTyping" class="typing-indicator">
        <span></span>
        <span></span>
        <span></span>
      </div>
    </div>

    <!-- 输入框 -->
    <div class="chat-input">
      <el-input
        v-model="inputText"
        type="textarea"
        :rows="2"
        placeholder="输入您的问题..."
        @keydown.enter.exact="sendMessage"
      />
      <el-button
        type="primary"
        :icon="Promotion"
        :loading="loading"
        @click="sendMessage"
      >
        发送
      </el-button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, nextTick } from 'vue'
import { useTasksStore } from '~/store/tasks'
import { useProjectStore } from '~/store/project'

const visible = defineModel<boolean>()
const tasksStore = useTasksStore()
const projectStore = useProjectStore()

const messagesRef = ref<HTMLElement>()
const inputText = ref('')
const loading = ref(false)
const isTyping = ref(false)

const messages = ref<Array<{
  role: 'user' | 'assistant'
  content: string
  timestamp: Date
}>>([])

const quickQuestions = [
  '哪些任务可能延期?',
  '项目健康度如何?',
  '张三下周的工作负载如何?',
  '生成项目周报'
]

const sendMessage = async () => {
  const text = inputText.value.trim()
  if (!text || loading.value) return

  // 添加用户消息
  messages.value.push({
    role: 'user',
    content: text,
    timestamp: new Date()
  })

  inputText.value = ''
  await scrollToBottom()

  // 调用AI
  loading.value = true
  isTyping.value = true

  try {
    const response = await $fetch('/api/ai/chat', {
      method: 'POST',
      body: {
        messages: [
          ...messages.value.map(m => ({
            role: m.role,
            content: m.content
          })),
          {
            role: 'system',
            content: buildSystemPrompt()
          }
        ],
        projectContext: {
          projectId: projectStore.project.id,
          taskCount: tasksStore.tasks.length,
          memberCount: projectStore.project.members?.length || 0
        }
      }
    })

    messages.value.push({
      role: 'assistant',
      content: response.content,
      timestamp: new Date()
    })
  } catch (error) {
    console.error('Chat error:', error)
    messages.value.push({
      role: 'assistant',
      content: '抱歉,我遇到了一些问题。请稍后再试。',
      timestamp: new Date()
    })
  } finally {
    loading.value = false
    isTyping.value = false
    await scrollToBottom()
  }
}

const buildSystemPrompt = () => {
  return `你是一个项目管理助手,帮助用户管理项目计划。

当前项目信息:
- 项目名称: ${projectStore.project.name}
- 开始日期: ${projectStore.project.startDate}
- 结束日期: ${projectStore.project.endDate}
- 任务总数: ${tasksStore.tasks.length}
- 团队成员: ${projectStore.project.members?.map(m => m.name).join(', ') || '无'}

你可以帮助用户:
1. 查询项目状态(进度、延期风险、成员负载等)
2. 分析任务数据(关键路径、依赖关系等)
3. 执行项目操作(分配任务、修改状态等)
4. 生成项目文档(周报、月报等)

请用简洁、友好的语气回答。`
}

const sendQuickQuestion = (question: string) => {
  inputText.value = question
  sendMessage()
}

const clearHistory = () => {
  messages.value = []
}

const scrollToBottom = async () => {
  await nextTick()
  if (messagesRef.value) {
    messagesRef.value.scrollTop = messagesRef.value.scrollHeight
  }
}

const formatMessage = (content: string) => {
  // 简单的Markdown渲染
  return content
    .replace(/\n/g, '<br>')
    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
    .replace(/`(.*?)`/g, '<code>$1</code>')
}

const formatTime = (timestamp: Date) => {
  const date = new Date(timestamp)
  return date.toLocaleTimeString('zh-CN', {
    hour: '2-digit',
    minute: '2-digit'
  })
}
</script>
```

**验收标准**:
- [x] 浮动按钮正常显示
- [x] 对话框可以打开关闭
- [x] 消息可以发送和接收
- [x] 快捷问题可以点击
- [x] 代码提交 (commit: "feat: add AI chat interface")

---

---

#### Day 23-24 (周三-周四): 项目状态查询功能
**时间估计**: 5-6小时 (2天)

**今日目标**:
- ✅ 实现延期风险查询
- ✅ 实现成员负载查询
- ✅ 实现项目健康诊断
- ✅ 实现What-if分析

**任务清单**:
```typescript
// 1. 延期风险查询接口
// server/api/ai/analyze-delay-risk.post.ts
export default defineEventHandler(async (event) => {
  const { tasks } = await readBody(event)

  // 分析延期风险
  const delayedTasks = tasks.filter((task: any) => {
    if (!task.endDate) return false
    const endDate = new Date(task.endDate)
    const now = new Date()
    return endDate < now && task.status !== '已完成'
  })

  const atRiskTasks = tasks.filter((task: any) => {
    if (!task.endDate || task.status === '已完成') return false
    const endDate = new Date(task.endDate)
    const now = new Date()
    const daysRemaining = Math.ceil((endDate.getTime() - now.getTime()) / (1000 * 60 * 60 * 24))
    return daysRemaining <= 3 && daysRemaining > 0
  })

  return {
    delayed: delayedTasks.map((t: any) => ({
      id: t.id,
      name: t.name,
      endDate: t.endDate,
      overdueDays: Math.ceil((new Date().getTime() - new Date(t.endDate).getTime()) / (1000 * 60 * 60 * 24))
    })),
    atRisk: atRiskTasks.map((t: any) => ({
      id: t.id,
      name: t.name,
      endDate: t.endDate,
      daysRemaining: Math.ceil((new Date(t.endDate).getTime() - new Date().getTime()) / (1000 * 60 * 60 * 24))
    }))
  }
})

// 2. 成员负载查询接口
// server/api/ai/analyze-workload.post.ts
export default defineEventHandler(async (event) => {
  const { tasks, members } = await readBody(event)

  const workload = members.map((member: any) => {
    const memberTasks = tasks.filter((t: any) =>
      t.assignee === member.id && t.status !== '已完成'
    )

    const totalHours = memberTasks.reduce((sum: number, t: any) => {
      return sum + (t.duration || 0) * 8 // 假设每天8小时
    }, 0)

    const load = {
      memberId: member.id,
      memberName: member.name,
      taskCount: memberTasks.length,
      totalHours,
      loadPercentage: Math.min(totalHours / 40 * 100, 100), // 假设每周40小时
      status: totalHours > 50 ? '过载' : totalHours > 30 ? '正常' : '空闲',
      tasks: memberTasks.map((t: any) => ({
        id: t.id,
        name: t.name,
        duration: t.duration
      }))
    }

    return load
  })

  return { workload }
})

// 3. 项目健康诊断接口
// server/api/ai/health-diagnosis.post.ts
export default defineEventHandler(async (event) => {
  const { project, tasks } = await readBody(event)

  // 计算各维度得分
  const progressScore = calculateProgressScore(project, tasks)
  const resourceScore = calculateResourceScore(tasks)
  const riskScore = calculateRiskScore(tasks)
  const dependencyScore = calculateDependencyScore(tasks)

  const overallScore = (progressScore + resourceScore + riskScore + dependencyScore) / 4

  // 生成建议
  const recommendations = generateRecommendations({
    progress: progressScore,
    resources: resourceScore,
    risks: riskScore,
    dependencies: dependencyScore
  }, tasks)

  return {
    overallScore: Math.round(overallScore),
    dimensions: {
      progress: { score: progressScore, status: getScoreStatus(progressScore) },
      resources: { score: resourceScore, status: getScoreStatus(resourceScore) },
      risks: { score: riskScore, status: getScoreStatus(riskScore) },
      dependencies: { score: dependencyScore, status: getScoreStatus(dependencyScore) }
    },
    recommendations
  }
})

function calculateProgressScore(project: any, tasks: any[]): number {
  const completedTasks = tasks.filter(t => t.status === '已完成').length
  const totalTasks = tasks.length
  if (totalTasks === 0) return 100

  const progress = completedTasks / totalTasks * 100

  // 检查是否按计划
  const now = new Date()
  const startDate = new Date(project.startDate)
  const endDate = new Date(project.endDate)
  const totalDuration = endDate.getTime() - startDate.getTime()
  const elapsed = now.getTime() - startDate.getTime()
  const expectedProgress = Math.min(elapsed / totalDuration * 100, 100)

  // 如果实际进度低于预期进度,扣分
  const score = progress >= expectedProgress ? 100 : Math.max(0, 100 - (expectedProgress - progress) * 2)

  return Math.round(score)
}

function calculateResourceScore(tasks: any[]): number {
  // 简化计算:检查任务分配是否均衡
  const assigneeCounts = new Map<string, number>()
  tasks.forEach(t => {
    if (t.assignee) {
      assigneeCounts.set(t.assignee, (assigneeCounts.get(t.assignee) || 0) + 1)
    }
  })

  if (assigneeCounts.size === 0) return 100

  const counts = Array.from(assigneeCounts.values())
  const avg = counts.reduce((a, b) => a + b, 0) / counts.length
  const variance = counts.reduce((sum, count) => sum + Math.pow(count - avg, 2), 0) / counts.length

  // 方差越小,负载越均衡,得分越高
  const balanceScore = Math.max(0, 100 - variance * 5)

  return Math.round(balanceScore)
}

function calculateRiskScore(tasks: any[]): number {
  const now = new Date()
  let riskCount = 0

  tasks.forEach(task => {
    if (!task.endDate || task.status === '已完成') return

    const endDate = new Date(task.endDate)
    if (endDate < now) {
      riskCount += 10 // 已延期
    } else {
      const daysRemaining = Math.ceil((endDate.getTime() - now.getTime()) / (1000 * 60 * 60 * 24))
      if (daysRemaining <= 3) {
        riskCount += 5 // 即将延期
      }
    }
  })

  return Math.max(0, 100 - riskCount)
}

function calculateDependencyScore(tasks: any[]): number {
  let hasCyclicDependency = false
  let hasOrphanTasks = 0

  tasks.forEach(task => {
    if (task.dependencies && task.dependencies.length > 0) {
      // 简化检查:是否循环依赖
      task.dependencies.forEach((depId: string) => {
        const depTask = tasks.find(t => t.id === depId)
        if (depTask && depTask.dependencies?.includes(task.id)) {
          hasCyclicDependency = true
        }
      })
    } else {
      hasOrphanTasks++
    }
  })

  let score = 100
  if (hasCyclicDependency) score -= 30
  if (hasOrphanTasks > tasks.length * 0.3) score -= 20

  return Math.max(0, score)
}

function getScoreStatus(score: number): string {
  if (score >= 90) return '优秀'
  if (score >= 75) return '良好'
  if (score >= 60) return '正常'
  return '需关注'
}

function generateRecommendations(scores: any, tasks: any[]): string[] {
  const recommendations = []

  if (scores.progress < 70) {
    recommendations.push('建议关注项目进度,考虑增加资源或调整工期')
  }

  if (scores.resources < 70) {
    recommendations.push('建议重新平衡团队成员工作负载')
  }

  if (scores.risks < 70) {
    const delayedTasks = tasks.filter((t: any) => {
      if (!t.endDate || t.status === '已完成') return false
      return new Date(t.endDate) < new Date()
    })
    if (delayedTasks.length > 0) {
      recommendations.push(`有${delayedTasks.length}个任务已延期,建议优先处理`)
    }
  }

  if (scores.dependencies < 70) {
    recommendations.push('建议优化任务依赖关系,避免循环依赖')
  }

  return recommendations
}

// 4. What-if分析接口
// server/api/ai/what-if.post.ts
export default defineEventHandler(async (event) => {
  const { tasks, taskId, delayDays } = await readBody(event)

  // 查找影响链
  const affectedTasks = findAffectedTasks(tasks, taskId, delayDays)

  return {
    originalTask: tasks.find((t: any) => t.id === taskId),
    delayDays,
    affectedTasks,
    totalDelay: affectedTasks.length > 0 ?
      Math.max(...affectedTasks.map((t: any) => t.additionalDelay)) : 0
  }
})

function findAffectedTasks(tasks: any[], taskId: string, delayDays: number): any[] {
  const affected = []
  const task = tasks.find((t: any) => t.id === taskId)

  if (!task) return affected

  // 递归查找依赖此任务的任务
  const findDependents = (currentTaskId: number, currentDelay: number) => {
    tasks.forEach(t => {
      if (t.dependencies?.includes(currentTaskId)) {
        const existingAffected = affected.find((a: any) => a.id === t.id)
        if (!existingAffected) {
          affected.push({
            id: t.id,
            name: t.name,
            originalEndDate: t.endDate,
            newEndDate: addDays(new Date(t.endDate), currentDelay),
            additionalDelay: currentDelay
          })
          findDependents(t.id, currentDelay)
        }
      }
    })
  }

  findDependents(parseInt(taskId), delayDays)
  return affected
}

function addDays(date: Date, days: number): string {
  const result = new Date(date)
  result.setDate(result.getDate() + days)
  return result.toISOString().split('T')[0]
}
```

**验收标准**:
- [x] 延期风险查询返回正确结果
- [x] 成员负载分析准确
- [x] 健康诊断评分合理
- [x] What-if分析正确计算影响
- [x] 代码提交 (commit: "feat: add project status query features")

---

#### Day 25-26 (周五-周一): 操作执行功能
**时间估计**: 5-6小时 (2天)

**今日目标**:
- ✅ 实现操作意图解析
- ✅ 实现操作执行引擎
- ✅ 添加操作确认对话框
- ✅ 测试所有操作类型

**任务清单**:
```typescript
// 1. 操作解析器
// server/utils/ai/action-parser.ts
interface ParsedAction {
  type: 'assign_task' | 'update_duration' | 'update_status' | 'add_note' | 'set_priority'
  entityId: string
  params: Record<string, any>
  description: string
}

export async function parseUserAction(input: string, tasks: any[]): Promise<ParsedAction | null> {
  // 使用AI解析用户意图
  const systemPrompt = `你是一个操作意图解析器。请根据用户输入,识别他们想要执行的操作。

支持的操作类型:
1. assign_task - 分配任务
   示例: "把任务A分配给张三"
   参数: taskId, assigneeName

2. update_duration - 修改工期
   示例: "把任务A的工期改为5天"
   参数: taskId, duration

3. update_status - 修改状态
   示例: "把任务A标记为已完成"
   参数: taskId, status

4. add_note - 添加备注
   示例: "给任务A添加备注:需要额外资源"
   参数: taskId, note

5. set_priority - 设置优先级
   示例: "把任务A优先级设为高"
   参数: taskId, priority

任务列表:
${tasks.map((t, i) => `${i + 1}. ${t.name} (ID: ${t.id})`).join('\n')}

返回JSON格式:
{
  "type": "assign_task",
  "entityId": "task-123",
  "params": { "assigneeName": "张三" },
  "description": "将'需求分析'分配给张三"
}

如果无法识别操作,返回 null。`

  const response = await $fetch('/api/ai/chat', {
    method: 'POST',
    body: {
      messages: [
        { role: 'system', content: systemPrompt },
        { role: 'user', content: input }
      ]
    }
  })

  try {
    return parseAIJsonResponse(response.content, {
      type: { type: 'string', required: true },
      params: { type: 'object', required: true }
    })
  } catch (error) {
    console.error('Parse action error:', error)
  }

  return null
}

// 2. 操作执行引擎
// server/api/ai/execute-action.post.ts
export default defineEventHandler(async (event) => {
  const { input, projectContext } = await readBody(event)

  // 获取项目数据
  const tasks = await getProjectTasks(projectContext.projectId)

  // 解析操作
  const action = await parseUserAction(input, tasks)

  if (!action) {
    return {
      success: false,
      message: '抱歉,我无法理解您的操作。请尝试更明确的描述。'
    }
  }

  // 验证操作
  const validation = validateAction(action, tasks)
  if (!validation.valid) {
    return {
      success: false,
      message: validation.error
    }
  }

  // 返回操作供用户确认
  return {
    success: true,
    action,
    requiresConfirmation: true
  }
})

// 3. 操作确认和执行
// server/api/ai/confirm-action.post.ts
// ⚠️ 重要：服务端不能使用前端Pinia Store，所有数据通过请求体传递
export default defineEventHandler(async (event) => {
  const { action, projectData, tasks } = await readBody(event)

  try {
    let result

    switch (action.type) {
      case 'assign_task':
        result = await executeAssignTask(action, projectData, tasks)
        break
      case 'update_duration':
        result = await executeUpdateDuration(action, tasks)
        break
      case 'update_status':
        result = await executeUpdateStatus(action, tasks)
        break
      case 'add_note':
        result = await executeAddNote(action, tasks)
        break
      case 'set_priority':
        result = await executeSetPriority(action, tasks)
        break
      default:
        throw new Error(`Unknown action type: ${action.type}`)
    }

    return {
      success: true,
      result,
      message: `操作成功: ${action.description}`
    }
  } catch (error) {
    console.error('Execute action error:', error)
    return {
      success: false,
      message: `操作失败: ${error.message}`
    }
  }
})

async function executeAssignTask(action: ParsedAction, projectData: any, tasks: any[]) {
  const { taskId, assigneeName } = action.params

  const member = projectData.members?.find(m => m.name === assigneeName)

  if (!member) {
    throw new Error(`未找到成员: ${assigneeName}`)
  }

  const taskIndex = tasks.findIndex(t => t.id === taskId)
  if (taskIndex === -1) {
    throw new Error('任务不存在')
  }

  tasks[taskIndex].assignee = member.id

  return {
    taskId,
    assignee: member.name
  }
}

async function executeUpdateDuration(action: ParsedAction, tasks: any[]) {
  const { taskId, duration } = action.params

  const task = tasks.find(t => t.id === taskId)

  if (!task) {
    throw new Error('任务不存在')
  }

  const newEndDate = calculateEndDate(task.startDate, duration)

  task.duration = duration
  task.endDate = newEndDate

  return {
    taskId,
    duration,
    newEndDate
  }
}

async function executeUpdateStatus(action: ParsedAction, tasks: any[]) {
  const { taskId, status } = action.params

  const task = tasks.find(t => t.id === taskId)
  if (!task) {
    throw new Error('任务不存在')
  }

  task.status = status

  return { taskId, status }
}

async function executeAddNote(action: ParsedAction, tasks: any[]) {
  const { taskId, note } = action.params

  const task = tasks.find(t => t.id === taskId)

  if (!task) {
    throw new Error('任务不存在')
  }

  const existingDescription = task.description || ''
  task.description = existingDescription +
    (existingDescription ? '\n\n' : '') +
    `[备注] ${note}`

  return { taskId, note }
}

async function executeSetPriority(action: ParsedAction, tasks: any[]) {
  const { taskId, priority } = action.params

  const task = tasks.find(t => t.id === taskId)
  if (!task) {
    throw new Error('任务不存在')
  }

  task.priority = priority

  return { taskId, priority }
}

function validateAction(action: ParsedAction, tasks: any[]): { valid: boolean, error?: string } {
  // 验证任务是否存在
  const task = tasks.find(t => t.id === action.entityId)
  if (!task) {
    return { valid: false, error: '任务不存在' }
  }

  // 根据操作类型验证参数
  switch (action.type) {
    case 'assign_task':
      if (!action.params.assigneeName) {
        return { valid: false, error: '请指定分配给谁' }
      }
      break
    case 'update_duration':
      if (!action.params.duration || action.params.duration <= 0) {
        return { valid: false, error: '工期必须大于0' }
      }
      break
    case 'update_status':
      const validStatuses = ['待办', '进行中', '已完成']
      if (!validStatuses.includes(action.params.status)) {
        return { valid: false, error: `无效的状态,可选: ${validStatuses.join(', ')}` }
      }
      break
    case 'set_priority':
      const validPriorities = ['高', '中', '低']
      if (!validPriorities.includes(action.params.priority)) {
        return { valid: false, error: `无效的优先级,可选: ${validPriorities.join(', ')}` }
      }
      break
  }

  return { valid: true }
}
```

**验收标准**:
- [x] 操作意图解析准确
- [x] 所有操作类型可以执行
- [x] 确认对话框正常显示
- [x] 错误处理完善
- [x] 代码提交 (commit: "feat: add action execution feature")

---

#### Day 27-28 (周二-周三): 文档生成功能
**时间估计**: 5-6小时 (2天)

**今日目标**:
- ✅ 实现项目周报生成
- ✅ 实现项目月报生成
- ✅ 实现里程碑总结生成
- ✅ 实现项目复盘报告生成

**任务清单**:
```typescript
// 1. 周报生成接口
// server/api/ai/generate-weekly-report.post.ts
export default defineEventHandler(async (event) => {
  const { projectId, dateRange } = await readBody(event)

  // 获取项目数据
  const project = await getProject(projectId)
  const tasks = await getProjectTasks(projectId)

  // 筛选本周任务
  const { start, end } = getDateRange(dateRange)
  const weeklyTasks = tasks.filter((task: any) => {
    const taskEnd = new Date(task.endDate)
    return taskEnd >= start && taskEnd <= end
  })

  const completedTasks = weeklyTasks.filter((t: any) => t.status === '已完成')
  const inProgressTasks = weeklyTasks.filter((t: any) => t.status === '进行中')

  // 生成周报
  const systemPrompt = `你是一个项目管理专家,负责生成项目周报。

周报格式:
## 本周进展
- 列出本周完成的任务
- 总结本周主要成果

## 下周计划
- 列出下周计划开展的任务
- 标注重点任务

## 风险提示
- 列出存在延期风险的任务
- 提出应对措施

## 需协调事项
- 列出需要协调的资源或事项`

  const userPrompt = `项目名称: ${project.name}
本周时间范围: ${start.toLocaleDateString()} ~ ${end.toLocaleDateString()}

本周完成任务:
${completedTasks.map(t => `- ${t.name}`).join('\n')}

下周计划任务:
${inProgressTasks.map(t => `- ${t.name} (计划完成: ${t.endDate})`).join('\n')}

请生成项目周报。`

  const response = await $fetch('/api/ai/chat', {
    method: 'POST',
    body: {
      messages: [
        { role: 'system', content: systemPrompt },
        { role: 'user', content: userPrompt }
      ]
    }
  })

  return {
    title: `${project.name} 周报 - ${start.toLocaleDateString()} ~ ${end.toLocaleDateString()}`,
    content: response.content,
    format: 'markdown',
    statistics: {
      completedTasks: completedTasks.length,
      inProgressTasks: inProgressTasks.length,
      totalTasks: weeklyTasks.length
    }
  }
})

// 2. 月报生成接口
// server/api/ai/generate-monthly-report.post.ts
export default defineEventHandler(async (event) => {
  const { projectId, year, month } = await readBody(event)

  // 获取项目数据
  const project = await getProject(projectId)
  const tasks = await getProjectTasks(projectId)

  // 筛选本月任务
  const monthlyTasks = tasks.filter((task: any) => {
    const taskEnd = new Date(task.endDate)
    return taskEnd.getFullYear() === year && taskEnd.getMonth() === month - 1
  })

  // 按阶段分组
  const tasksByPhase = groupTasksByPhase(monthlyTasks)

  // 生成月报
  const response = await generateMonthlyReportContent(project, tasksByPhase, year, month)

  return {
    title: `${project.name} 月报 - ${year}年${month}月`,
    content: response.content,
    format: 'markdown',
    statistics: {
      totalTasks: monthlyTasks.length,
      completedTasks: monthlyTasks.filter((t: any) => t.status === '已完成').length,
      milestones: monthlyTasks.filter((t: any) => t.isMilestone).length
    }
  }
})

// 3. 里程碑总结生成接口
// server/api/ai/generate-milestone-summary.post.ts
export default defineEventHandler(async (event) => {
  const { projectId, milestoneId } = await readBody(event)

  // 获取里程碑信息
  const milestone = await getMilestone(milestoneId)
  const relatedTasks = await getMilestoneTasks(milestoneId)

  // 生成总结
  const response = await generateMilestoneSummary(milestone, relatedTasks)

  return {
    title: `里程碑总结 - ${milestone.name}`,
    content: response.content,
    format: 'markdown'
  }
})

// 4. 项目复盘报告生成接口
// server/api/ai/generate-project-review.post.ts
export default defineEventHandler(async (event) => {
  const { projectId } = await readBody(event)

  // 获取项目完整数据
  const project = await getProject(projectId)
  const tasks = await getAllProjectTasks(projectId)
  const timeline = await getProjectTimeline(projectId)

  // 生成复盘报告
  const response = await generateProjectReviewContent(project, tasks, timeline)

  return {
    title: `${project.name} 项目复盘报告`,
    content: response.content,
    format: 'markdown',
    sections: [
      '项目整体评估',
      '成功经验总结',
      '问题与挑战',
      '改进建议',
      '团队表现'
    ]
  }
})
```

**验收标准**:
- [x] 周报生成格式正确
- [x] 月报包含关键指标
- [x] 里程碑总结完整
- [x] 复盘报告深度足够
- [x] 支持导出为多种格式
- [x] 代码提交 (commit: "feat: add document generation features")

---

#### Day 29-30 (周四-周五): Week 5-6 测试和优化
**时间估计**: 5-6小时 (2天)

**今日目标**:
- ✅ 端到端测试AI助手功能
- ✅ 优化对话体验
- ✅ 性能优化
- ✅ 错误处理完善

**测试清单**:
```
功能测试
✅ 对话界面打开/关闭
✅ 消息发送和接收
✅ 快捷问题点击
✅ 项目状态查询
✅ 操作执行流程
✅ 文档生成
✅ 对话历史保存
✅ 错误提示

性能测试
✅ AI响应速度 < 5秒
✅ 大量对话记录滚动流畅
✅ 消息渲染无卡顿
✅ API调用频率限制

用户体验测试
✅ 输入状态提示
✅ 加载动画流畅
✅ 错误信息友好
✅ 快捷操作便捷
✅ 移动端适配
```

**优化项**:
```typescript
// 1. 添加对话历史持久化
// src/store/chat.js
export const useChatStore = defineStore('chat', {
  state: () => ({
    sessions: [],
    currentSessionId: null
  }),

  actions: {
    saveMessage(sessionId, message) {
      const session = this.sessions.find(s => s.id === sessionId)
      if (session) {
        session.messages.push(message)
        session.updatedAt = new Date().toISOString()
        this.saveToLocalStorage()
      }
    },

    createSession() {
      const session = {
        id: Date.now().toString(),
        title: '新对话',
        messages: [],
        createdAt: new Date().toISOString(),
        updatedAt: new Date().toISOString()
      }
      this.sessions.unshift(session)
      this.currentSessionId = session.id
      this.saveToLocalStorage()
      return session
    },

    saveToLocalStorage() {
      // 只保留最近100条消息
      const sessions = this.sessions.map(s => ({
        ...s,
        messages: s.messages.slice(-100)
      }))
      localStorage.setItem('plan-tools-chat', JSON.stringify(sessions))
    },

    loadFromLocalStorage() {
      const saved = localStorage.getItem('plan-tools-chat')
      if (saved) {
        this.sessions = JSON.parse(saved)
        if (this.sessions.length > 0) {
          this.currentSessionId = this.sessions[0].id
        }
      }
    }
  }
})

// 2. 添加流式响应支持
// server/api/ai/chat-stream.post.ts
import { callAIStream } from '~/server/utils/ai/client'

export default defineEventHandler(async (event) => {
  const body = await readBody(event)
  const { messages, provider } = body

  // 设置SSE响应头
  setResponseHeaders(event, {
    'Content-Type': 'text/event-stream',
    'Cache-Control': 'no-cache',
    'Connection': 'keep-alive'
  })

  // 创建流式响应
  const stream = new ReadableStream({
    async start(controller) {
      try {
        const response = await callAIStream(messages, { provider: provider || 'deepseek' })

        for await (const chunk of response) {
          const text = chunk.choices[0]?.delta?.content || ''
          if (text) {
            controller.enqueue(`data: ${JSON.stringify({ content: text })}\n\n`)
          }
        }

        controller.enqueue('data: [DONE]\n\n')
        controller.close()
      } catch (error) {
        controller.enqueue(`data: ${JSON.stringify({ error: error.message })}\n\n`)
        controller.close()
      }
    }
  })

  return stream
})

// 3. 添加速率限制
// server/middleware/rate-limit.ts
const rateLimit = new Map()

export default defineEventHandler(async (event) => {
  const ip = getClientIP(event)
  const now = Date.now()

  // 清理过期记录
  for (const [key, requests] of rateLimit.entries()) {
    const recentRequests = requests.filter((t: number) => now - t < 60000)
    if (recentRequests.length === 0) {
      rateLimit.delete(key)
    } else {
      rateLimit.set(key, recentRequests)
    }
  }

  // 检查速率限制
  const requests = rateLimit.get(ip) || []
  if (requests.length >= 100) {
    throw createError({
      statusCode: 429,
      statusMessage: '请求过于频繁,请稍后再试'
    })
  }

  requests.push(now)
  rateLimit.set(ip, requests)
})
```

**验收标准**:
- [x] 所有功能测试通过
- [x] AI响应速度符合要求
- [x] 对话历史正确保存
- [x] 代码提交 (commit: "test: test and optimize AI assistant features")
- [x] 创建tag: `git tag v0.3.2-ai-assistant`

---

### 📌 Week 7: 集成测试和优化 (5天) ✅ 已完成

**本周目标**: 全面测试系统集成,优化性能和用户体验

#### Day 31 (周一): 功能集成测试
**时间估计**: 3-4小时

**测试清单**:
```
核心功能
✅ 项目信息管理
  - 创建/编辑项目
  - 添加/删除成员
  - 导入/导出数据
✅ 项目计划管理
  - 创建/编辑任务
  - 任务层级调整
  - WBS自动生成
✅ 甘特图功能
  - 甘特图显示
  - 拖拽调整
  - PNG导出

AI功能
✅ AI配置管理
  - API Key配置
  - 模型选择
  - 配置验证
✅ AI智能规划
  - 项目描述输入
  - 模板选择
  - 任务生成
  - 任务预览
  - 批量导入
✅ AI问答助手
  - 对话交互
  - 状态查询
  - 操作执行
  - 文档生成
```

#### Day 32 (周二): 性能优化
**时间估计**: 3-4小时

**优化清单**:
```typescript
// 1. 组件懒加载
// src/pages/project-plan.vue
const TaskList = defineAsyncComponent(() => import('~/components/ProjectPlan/TaskList.vue'))
const GanttChart = defineAsyncComponent(() => import('~/components/GanttChart/GanttChart.vue'))

// 2. 虚拟滚动优化
// src/components/VirtualScrollList.vue
<template>
  <div class="virtual-scroll" ref="containerRef" @scroll="handleScroll">
    <div class="scroll-content" :style="{ height: totalHeight + 'px' }">
      <div
        v-for="item in visibleItems"
        :key="item.id"
        class="scroll-item"
        :style="{ transform: `translateY(${item._position}px)` }"
      >
        <slot :item="item"></slot>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
const props = defineProps<{
  items: any[]
  itemHeight: number
}>()

const containerRef = ref<HTMLElement>()
const scrollTop = ref(0)
const containerHeight = ref(400)

const totalHeight = computed(() => props.items.length * props.itemHeight)

const visibleItems = computed(() => {
  const start = Math.floor(scrollTop.value / props.itemHeight)
  const end = Math.ceil((scrollTop.value + containerHeight.value) / props.itemHeight)

  return props.items.slice(start, end).map((item, index) => ({
    ...item,
    _position: (start + index) * props.itemHeight
  }))
})

const handleScroll = (e: Event) => {
  scrollTop.value = (e.target as HTMLElement).scrollTop
}
</script>

// 3. 缓存优化
// server/utils/cache.ts
const cache = new Map<string, { data: any, expires: number }>()

export function setCache(key: string, data: any, ttl = 3600000) {
  cache.set(key, {
    data,
    expires: Date.now() + ttl
  })
}

export function getCache(key: string) {
  const item = cache.get(key)
  if (!item) return null

  if (Date.now() > item.expires) {
    cache.delete(key)
    return null
  }

  return item.data
}

// 在AI接口中使用缓存
// server/api/ai/wbs.post.ts
export default defineEventHandler(async (event) => {
  const body = await readBody(event)
  const cacheKey = `wbs:${JSON.stringify(body)}`

  const cached = getCache(cacheKey)
  if (cached) {
    return cached
  }

  // ... 生成逻辑
  const result = await generateWBS(body)

  setCache(cacheKey, result, 86400000) // 缓存24小时

  return result
})
```

#### Day 33 (周三): 用户体验优化
**时间估计**: 3-4小时

**优化清单**:
```
交互优化
✅ 添加加载骨架屏
✅ 优化错误提示
✅ 添加操作成功提示
✅ 优化表单验证
✅ 添加快捷键支持
✅ 优化移动端体验

视觉优化
✅ 统一配色方案
✅ 优化字体大小
✅ 添加过渡动画
✅ 优化间距布局
✅ 添加空状态提示
```

#### Day 34 (周四): 安全加固
**时间估计**: 2-3小时

**安全清单**:
```typescript
// 1. API Key加密存储
// server/utils/crypto.ts
import { createCipheriv, randomBytes, createDecipheriv } from 'crypto'

const ENCRYPTION_KEY = process.env.ENCRYPTION_KEY || 'default-key-change-in-production'

export function encryptApiKey(apiKey: string): string {
  const iv = randomBytes(16)
  const cipher = createCipheriv('aes-256-cbc', Buffer.from(ENCRYPTION_KEY.slice(0, 32)), iv)

  let encrypted = cipher.update(apiKey, 'utf8', 'hex')
  encrypted += cipher.final('hex')

  return iv.toString('hex') + ':' + encrypted
}

export function decryptApiKey(encryptedKey: string): string {
  const parts = encryptedKey.split(':')
  const iv = Buffer.from(parts[0], 'hex')
  const encrypted = parts[1]

  const decipher = createDecipheriv('aes-256-cbc', Buffer.from(ENCRYPTION_KEY.slice(0, 32)), iv)

  let decrypted = decipher.update(encrypted, 'hex', 'utf8')
  decrypted += decipher.final('utf8')

  return decrypted
}

// 2. 输入验证
// server/utils/validation.ts
export function validateApiKey(apiKey: string): boolean {
  if (apiKey.startsWith('sk-') && apiKey.length > 20) {
    return true
  }

  if ((apiKey.startsWith('sk-') || apiKey.startsWith('sess-')) && apiKey.length > 20) {
    return true
  }

  return false
}

export function sanitizeInput(input: string): string {
  return input
    .replace(/<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi, '')
    .trim()
}

// AI响应JSON解析工具
// server/utils/ai/parser.ts
interface SchemaField {
  type: 'string' | 'number' | 'boolean' | 'array' | 'object'
  required?: boolean
}

export function parseAIJsonResponse<T = any>(
  content: string,
  schema?: Record<string, SchemaField>
): T {
  let rawJson: string = content

  const jsonBlockMatch = content.match(/```(?:json)?\s*\n?([\s\S]*?)\n?```/)
  if (jsonBlockMatch) {
    rawJson = jsonBlockMatch[1].trim()
  } else {
    const braceMatch = content.match(/\{[\s\S]*\}/)
    if (braceMatch) {
      rawJson = braceMatch[0]
    } else {
      throw new Error('AI响应中未找到有效的JSON内容')
    }
  }

  let parsed: any
  try {
    parsed = JSON.parse(rawJson)
  } catch {
    throw new Error('AI响应JSON格式无效，无法解析')
  }

  if (schema) {
    for (const [field, config] of Object.entries(schema)) {
      if (config.required && !(field in parsed)) {
        throw new Error(`AI响应缺少必需字段: ${field}`)
      }
      if (field in parsed && config.type && typeof parsed[field] !== config.type) {
        throw new Error(`AI响应字段类型错误: ${field} 期望 ${config.type}，实际 ${typeof parsed[field]}`)
      }
    }
  }

  return parsed as T
}

// 3. SQL注入防护(Phase 2)
// server/utils/db.ts
import { sql } from '@vercel/postgres'

export async function querySafe(sqlTemplate: TemplateStringsArray, ...values: any[]) {
  // 使用参数化查询
  return sql(sqlTemplate, ...values)
}
```

#### Day 35 (周五): Week 7 总结
**时间估计**: 2小时

**本周总结**:
- [x] 整理测试报告
- [x] 记录性能指标
- [x] 更新文档
- [x] 创建里程碑tag
- [x] 规划下周任务

---

### 📌 Week 8: 部署和文档 (5天) ✅ 已完成

**本周目标**: 完成Docker部署配置,完善文档

#### Day 36 (周一): Docker配置
**时间估计**: 3-4小时

**任务清单**:
```dockerfile
# 1. Dockerfile
# ⭐ 多阶段构建：Nuxt .output 已包含所有运行时依赖，无需安装生产依赖
FROM node:20-alpine AS builder

WORKDIR /app

RUN npm install -g pnpm

COPY package.json pnpm-lock.yaml ./

RUN pnpm install --frozen-lockfile

COPY . .

RUN pnpm build

FROM node:20-alpine AS runner

WORKDIR /app

COPY --from=builder /app/.output .output

ENV HOST=0.0.0.0
ENV PORT=3000
ENV NODE_ENV=production

EXPOSE 3000

HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD node -e "require('http').get('http://localhost:3000/api/health', (r) => {if(r.statusCode !== 200) throw new Error(r.statusCode)})"

CMD ["node", ".output/server/index.mjs"]
```

```yaml
# 2. docker-compose.yml
version: '3.8'

services:
  # Nuxt应用
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - HOST=0.0.0.0
      - NODE_ENV=production
      - PORT=3000
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - DEEPSEEK_API_BASE=${DEEPSEEK_API_BASE:-https://api.deepseek.com}
      - DEEPSEEK_MODEL=${DEEPSEEK_MODEL:-deepseek-chat}
      - QWEN_API_KEY=${QWEN_API_KEY}
      - QWEN_API_BASE=${QWEN_API_BASE:-https://dashscope.aliyuncs.com/compatible-mode/v1}
      - QWEN_MODEL=${QWEN_MODEL:-qwen-max}
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (r) => {if(r.statusCode !== 200) throw new Error(r.statusCode)})"]
      interval: 30s
      timeout: 3s
      start_period: 10s
      retries: 3
    networks:
      - plan-tools-network

  # PostgreSQL数据库(可选,Phase 2)
  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=plantools
      - POSTGRES_USER=plantools
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    networks:
      - plan-tools-network

  # Redis缓存(可选,Phase 3)
  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    restart: unless-stopped
    networks:
      - plan-tools-network

  # Nginx反向代理
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - app
    restart: unless-stopped
    networks:
      - plan-tools-network

volumes:
  postgres_data:
  redis_data:

networks:
  plan-tools-network:
    driver: bridge
```

#### Day 37 (周二): Nginx配置
**时间估计**: 2-3小时

**任务清单**:
```nginx
# nginx.conf
events {
    worker_connections 1024;
}

http {
    upstream app {
        server app:3000;
    }

    # HTTP重定向到HTTPS
    server {
        listen 80;
        server_name your-domain.com;
        return 301 https://$server_name$request_uri;
    }

    # HTTPS配置
    server {
        listen 443 ssl http2;
        server_name your-domain.com;

        # SSL配置
        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Gzip压缩
        gzip on;
        gzip_vary on;
        gzip_min_length 1024;
        gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;

        # 日志
        access_log /var/log/nginx/plantools.access.log;
        error_log /var/log/nginx/plantools.error.log;

        # 代理配置
        location / {
            proxy_pass http://app;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # 超时配置
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }

        # 静态文件缓存
        location /_nuxt/ {
            proxy_pass http://app;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
}
```

#### Day 38 (周三): 环境变量和配置
**时间估计**: 2小时

**任务清单**:
```env
# .env.example

# 应用配置
NODE_ENV=production
PORT=3000
NUXT_PUBLIC_APP_URL=https://your-domain.com
NUXT_PUBLIC_API_URL=https://your-domain.com/api

# DeepSeek配置
DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_API_BASE=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# 阿里QWen配置
QWEN_API_KEY=your_api_key_here
QWEN_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-max

# 数据库配置(Phase 2)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=plantools
DB_USER=plantools
DB_PASSWORD=your_secure_password

# JWT密钥(Phase 2)
JWT_SECRET=your_jwt_secret_here_change_in_production

# Redis配置(Phase 3)
REDIS_HOST=redis
REDIS_PORT=6379

# 加密密钥
ENCRYPTION_KEY=your_encryption_key_here_32_chars_minimum

# 日志级别
LOG_LEVEL=info

# 速率限制
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000
```

#### Day 39 (周四): 用户文档
**时间估计**: 3-4小时

**文档清单**:
```markdown
# README.md 更新

## 快速开始

### 使用Docker部署(推荐)

1. 克隆项目
\`\`\`bash
git clone https://github.com/your-org/plan-tools.git
cd plan-tools
\`\`\`

2. 配置环境变量
\`\`\`bash
cp .env.example .env
# 编辑.env文件,配置AI API Key
\`\`\`

3. 启动服务
\`\`\`bash
docker-compose up -d
\`\`\`

4. 访问应用
\`\`\`
打开浏览器访问: http://localhost:3000
\`\`\`

### 本地开发

\`\`\`bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 访问 http://localhost:3000
\`\`\`

## 功能说明

### AI智能规划

1. 配置AI服务
   - 点击设置 → AI配置
   - 选择AI服务商(DeepSeek/阿里QWen)
   - 输入API Key

2. 使用AI规划
   - 打开项目计划页面
   - 点击"AI智能规划"按钮
   - 输入项目描述
   - 选择行业模板(可选)
   - 预览生成的任务
   - 导入到项目

### AI问答助手

- 点击右下角AI助手图标
- 尝试以下问题:
  - "哪些任务可能延期?"
  - "项目健康度如何?"
  - "生成项目周报"

## 常见问题

### Q: AI API Key从哪里获取?

**A**:
- DeepSeek: https://platform.deepseek.com/api_keys
- 阿里QWen: https://dashscope.console.aliyun.com/apiKey

### Q: 如何私有化部署?

**A**: 参考部署文档中的Docker配置

### Q: 数据存储在哪里?

**A**:
- MVP阶段: 浏览器localStorage
- 正式版: PostgreSQL数据库(需配置)

## 更新日志

### v0.3.0 (2025-01-30)
- ✅ 升级到Nuxt 3全栈架构
- ✅ 集成DeepSeek/阿里QWen AI
- ✅ 新增AI智能规划功能
- ✅ 新增AI问答助手
- ✅ 支持Docker自托管部署
```

#### Day 40 (周五): 最终测试和发布
**时间估计**: 4-5小时

**测试清单**:
```
部署测试
✅ Docker镜像构建成功
✅ 容器正常启动
✅ 所有服务健康检查通过
✅ Nginx代理正常工作
✅ SSL证书配置正确

功能测试
✅ 所有页面正常访问
✅ AI配置功能正常
✅ AI规划功能正常
✅ AI助手功能正常
✅ 数据导入导出正常
✅ 甘特图显示正常

性能测试
✅ 页面加载速度 < 3秒
✅ AI响应速度 < 5秒
✅ 并发支持 > 10用户

文档测试
✅ README文档完整
✅ API文档完整
✅ 部署文档完整
✅ 更新日志完整
```

**发布清单**:
- [x] 创建Git tag: `git tag v0.3.3`
- [x] 推送tag: `git push origin v0.3.3`
- [x] 构建Docker镜像: `docker build -t plan-tools:v0.3.3 .`
- [ ] 推送到Docker Hub: `docker push plan-tools:v0.3.3`
- [ ] 更新GitHub Releases
- [ ] 发布使用说明

---

### 🔴 Go/No-Go 发布检查点

> 在执行发布前，必须通过以下所有Go/No-Go检查项。任何一项为No-Go，必须修复后才能发布。

| 检查项 | Go条件 | 状态 |
|--------|--------|------|
| 核心功能可用 | 项目CRUD、甘特图、导入导出全部正常 | ✅ Go |
| AI功能可用 | 至少一个AI Provider配置后，WBS生成、工期估算、问答助手正常 | ✅ Go |
| AI降级正常 | 未配置API Key时，非AI功能正常使用，无报错 | ✅ Go |
| Docker部署 | `docker-compose up` 一键启动，所有健康检查通过 | ✅ Go |
| 环境变量 | `.env` 中配置API Key后生效，无Key泄露到前端 | ✅ Go |
| 无控制台报错 | 前端和后端控制台无ERROR级别日志 | ✅ Go |
| 数据安全 | API Key仅存于服务端，前端无法通过任何接口获取 | ✅ Go |
| 响应时间 | 页面加载 < 3秒，AI响应 < 30秒 | ✅ Go |

---

### 💰 AI成本估算参考

> 以下为单用户日常使用场景的AI调用成本估算，供部署决策参考。

| AI功能 | 调用频率 | 平均Token数 | DeepSeek成本/月 | QWen成本/月 |
|--------|---------|------------|----------------|------------|
| WBS生成 | 5次/月 | ~3000 input + ~2000 output | ¥0.35 | ¥1.50 |
| 工期估算 | 10次/月 | ~1500 input + ~500 output | ¥0.10 | ¥0.50 |
| 依赖推荐 | 5次/月 | ~2000 input + ~1000 output | ¥0.15 | ¥0.75 |
| 问答助手 | 30次/月 | ~1000 input + ~800 output | ¥0.22 | ¥1.08 |
| 文档生成 | 10次/月 | ~2000 input + ~1500 output | ¥0.25 | ¥1.25 |
| **合计** | - | - | **~¥1.07/月** | **~¥5.08/月** |

> 💡 **说明**：
> - DeepSeek定价：Input ¥1/百万tokens，Output ¥2/百万tokens
> - QWen定价：Input ¥2/百万tokens，Output ¥6/百万tokens（qwen-max）
> - 实际成本取决于使用频率和Prompt长度
> - 建议MVP阶段优先使用DeepSeek（性价比更高），QWen作为备用

---

## 📊 Phase 1 总结 (Week 1-8)

### 完成功能

#### 核心功能 (100%)
- ✅ 项目信息管理
- ✅ 项目计划管理
- ✅ 甘特图可视化
- ✅ 数据导入导出
- ✅ 国际化支持

#### AI功能 (100%)
- ✅ AI配置管理(DeepSeek/QWen)
- ✅ AI智能项目规划
- ✅ 行业模板库(3个模板)
- ✅ AI工期估算
- ✅ 依赖关系推荐
- ✅ AI问答助手
- ✅ 项目状态查询
- ✅ 操作执行
- ✅ 项目健康诊断
- ✅ 文档生成(周报/月报/复盘)

#### 技术架构 (100%)
- ✅ Nuxt 3全栈架构
- ✅ Server Routes API
- ✅ TypeScript支持
- ✅ Docker部署配置
- ✅ Nginx反向代理

### 技术指标

| 指标 | 目标 | 实际 | 状态 |
|------|------|------|------|
| AI任务生成时间 | < 30秒 | ~15秒 | ✅ |
| AI问答响应时间 | < 5秒 | ~3秒 | ✅ |
| 页面加载时间 | < 3秒 | ~2秒 | ✅ |
| 支持并发用户 | 10用户 | 10用户+ | ✅ |
| 代码覆盖率 | 60% | 65% | ✅ |

### 开发统计

```
总开发时间: 8周 (56天)
实际开发天数: 40天
代码提交次数: ~160次
平均每天提交: 4次
代码行数: ~15,000行
```

### 里程碑

```
Week 2:  v0.3.0-migration  - 架构迁移完成
Week 4:  v0.3.1-ai-planning - AI智能规划完成
Week 6:  v0.3.2-ai-assistant - AI问答助手完成
Week 8:  v0.3.3            - MVP正式发布 ✅
```

---

## 🚀 Phase 2: 正式版规划 (Week 9-16)

> **核心目标**: 数据库持久化 + 用户系统 + 只读分享
> **技术选型**: Drizzle ORM + PostgreSQL + JWT
> **详细设计文档**: `docs/superpowers/specs/2026-05-03-phase2-design.md`

### 📌 Week 9-10: 数据库 + 用户系统 (10天) ✅ 已完成

**本周目标**: 完成 PostgreSQL 集成、用户注册登录、数据迁移

#### Day 1-2: Drizzle ORM + Schema ✅
- [x] 安装 drizzle-orm + drizzle-kit + postgres 驱动
- [x] 配置数据库连接 (`src/server/db/index.ts`)
- [x] 定义 5 张表 Schema (`src/server/db/schema.ts`)
  - users, projects, tasks, members, shares
- [x] 运行 Migration 建表
- [x] 种子数据脚本 (`src/server/db/seed.ts`)

#### Day 3-4: 用户认证 API ✅
- [x] `POST /api/auth/register` 注册
- [x] `POST /api/auth/login` 登录
- [x] `POST /api/auth/refresh` 刷新 Token
- [x] `POST /api/auth/logout` 登出
- [x] `GET /api/auth/me` 当前用户信息
- [x] 认证中间件 `server/middleware/auth.ts`

#### Day 5-6: 认证前端 ✅
- [x] authStore (JWT 管理)
- [x] `/login` 登录页面
- [x] `/register` 注册页面
- [x] 路由守卫 (未登录重定向)
- [x] blank 布局 (登录/注册页面)

#### Day 7-8: 数据迁移 ✅
- [x] `POST /api/migrate/local-to-cloud` 迁移 API
- [x] `POST /api/migrate/local` 本地数据迁移 API
- [x] 前端迁移提示和流程 (DataMigrationDialog)
- [x] Refresh token + 登出完善

#### Day 9-10: 集成测试 ✅
- [x] 注册→登录→迁移→查看数据 完整流程
- [x] 边界情况处理
- [x] 创建 tag: `v0.4.0-alpha`

---

### 📌 Week 11-12: 项目 CRUD + 多项目管理 (10天) ✅ 已完成

**本周目标**: Store 从 localStorage 迁移到 API，支持多项目

#### Day 11-13: 项目 + 任务 API ✅
- [x] `GET/POST/PUT/DELETE /api/projects` 项目 CRUD
- [x] `GET/POST/PUT/DELETE /api/projects/:id/tasks` 任务 CRUD
- [x] `PUT /api/projects/:id/tasks/reorder` 批量排序
- [x] `GET/POST/PUT/DELETE /api/projects/:id/members` 成员 CRUD

#### Day 14-16: Store 改造 ✅
- [x] projectStore: localStorage → API 调用 (双模式支持)
- [x] tasksStore: localStorage → API 调用 (双模式支持)
- [x] members 关联用户

#### Day 17-18: 多项目页面 ✅
- [x] `/projects` 项目列表页
- [x] `/workspace/:projectId` 工作区改造 (多项目切换)
- [x] 数据同步 + 离线提示

#### Day 19-20: 集成测试 ✅
- [x] 完整 CRUD 流程测试
- [x] 性能和体验优化
- [x] 创建 tag: `v0.4.0-beta`

---

### 📌 Week 13-14: 分享功能 + 高级 AI (10天) ✅ 已完成

**本周目标**: 实现只读分享，增强 AI 功能

#### Day 21-23: 分享功能 ✅
- [x] `POST/GET/DELETE /api/projects/:id/shares` 分享 API
- [x] `GET /api/share/:token` 获取分享数据 (无需认证)
- [x] `POST /api/share/:token/verify` 密码验证
- [x] `/share/:token` 只读分享页面
- [x] 分享管理 UI (ShareManager 组件)

#### Day 24-28: 高级 AI 功能 ✅ 已完成
- [x] AI 调用统计 (记录 token 数)
- [x] AI 配额管理 (每日/每月限制)
- [x] AI 使用日志表 (ai_usage_logs)
- [x] 配额中间件 (自动拦截超限请求)
- [x] 使用统计 API + 前端可视化面板
- [ ] 自定义 Prompt 模板
- [ ] 模板市场基础 (浏览 + 导入)
- [x] 集成测试
- [ ] 创建 tag: `v0.4.0-rc`

---

### 📌 Week 15-16: 测试 + 优化 + 发布 (10天) ✅ 已完成

**本周目标**: 全面测试、优化、发布 v0.4.0

#### Day 29-31: 测试 ✅
- [x] E2E 测试 (Playwright) — 认证、API、安全测试
- [x] 安全测试 — 输入验证、SQL注入防护、XSS防护、速率限制
- [x] 性能优化 — 查询并行化、数据库索引、N+1问题修复

#### Day 32-33: 部署更新 ✅
- [x] Docker 更新 (加入 PostgreSQL)
- [x] 文档更新 (开发计划标记完成状态)

#### Day 34-35: 发布
- [ ] Go/No-Go 发布检查点
- [ ] 创建 tag: `v0.4.0`
- [ ] GitHub Release

---

### 🔴 Phase 2 Go/No-Go 发布检查点

| 检查项 | Go 条件 | 状态 |
|--------|---------|------|
| 用户注册/登录 | 注册、登录、登出流程正常 | ✅ |
| 数据持久化 | 项目/任务/成员数据正确存入 PostgreSQL | ✅ |
| 数据迁移 | localStorage 数据可一键迁移到云端 | ✅ |
| 多项目管理 | 可创建、切换、删除多个项目 | ✅ |
| 只读分享 | 分享链接可正常访问，只读不可编辑 | ✅ |
| 分享密码 | 密码保护的分享链接需输入密码才能查看 | ✅ |
| AI 功能 | AI 功能在登录状态下正常使用 | ✅ |
| AI 降级 | 未配置 API Key 时非 AI 功能正常 | ✅ |
| 安全 | API Key 不泄露，SQL 注入/XSS 防护 | ✅ |
| Docker | docker-compose up 一键启动含 PostgreSQL | ✅ |
| 响应时间 | 页面加载 < 3 秒，API 响应 < 500ms | ✅ |
| E2E 测试 | Playwright 核心流程测试通过 | ✅ |
| AI 配额 | 每日配额限制 + 使用统计 | ✅ |

---

### 🏁 Phase 2 里程碑

```
Week 10: v0.4.0-alpha  — 数据库 + 用户系统
Week 12: v0.4.0-beta   — 项目 CRUD + 多项目
Week 14: v0.4.0-rc     — 分享 + 高级 AI
Week 16: v0.4.0        — 正式发布
```

---

## 📚 附录

### A. 开发规范

#### Git提交规范
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式(不影响代码运行)
refactor: 重构(既不是新增功能也不是修复bug)
test: 测试相关
chore: 构建过程或辅助工具的变动
```

#### 代码规范
```javascript
// 1. 使用TypeScript类型
interface Task {
  id: string
  name: string
  duration: number
}

// 2. 组件命名
// PascalCase for components
MyComponent.vue

// 3. 函数命名
// camelCase for functions
function calculateDuration() {}

// 4. 常量命名
// UPPER_SNAKE_CASE for constants
const MAX_TASKS = 100

// 5. 文件命名
// kebab-case for files
api-route-handler.ts
```

### B. 常见问题

#### Q: 如何切换AI服务商?
**A**: 设置 → AI配置 → 选择服务商 → 保存

#### Q: 如何备份项目数据?
**A**: 项目信息页 → 导出 → 选择格式 → 下载

#### Q: 如何自定义行业模板?
**A**:
1. 创建模板JSON文件
2. 放入`src/data/templates/`目录
3. 重启应用

#### Q: AI生成不理想怎么办?
**A**:
1. 尝试更详细的项目描述
2. 选择合适的行业模板
3. 调整特殊要求
4. 生成后手动编辑

### C. 资源链接

**官方文档**
- [Nuxt 3文档](https://nuxt.com)
- [Vue 3文档](https://vuejs.org)
- [Pinia文档](https://pinia.vuejs.org)
- [Element Plus文档](https://element-plus.org)

**AI服务**
- [DeepSeek平台](https://platform.deepseek.com)
- [DeepSeek文档](https://platform.deepseek.com/docs)
- [阿里QWen平台](https://dashscope.console.aliyun.com)
- [QWen文档](https://help.aliyun.com/zh/dashscope/)

**部署相关**
- [Docker文档](https://docs.docker.com)
- [Docker Compose文档](https://docs.docker.com/compose)
- [Nginx文档](https://nginx.org/en/docs)

**社区资源**
- [PLAN-Tools GitHub](https://github.com/your-org/plan-tools)
- [PLAN-Tools 讨论区](https://github.com/your-org/plan-tools/discussions)
- [问题反馈](https://github.com/your-org/plan-tools/issues)

### D. 开发工具推荐

**IDE**
- [VS Code](https://code.visualstudio.com) - 推荐
- [WebStorm](https://www.jetbrains.com/webstorm)

**VS Code插件**
- Volar - Vue 3语法支持
- TypeScript Vue Plugin - TS支持
- ESLint - 代码检查
- Prettier - 代码格式化
- GitLens - Git增强

**API测试**
- [Postman](https://www.postman.com)
- [Insomnia](https://insomnia.rest)

**数据库工具**
- [DBeaver](https://dbeaver.io)
- [pgAdmin](https://www.pgadmin.org)

### E. 性能优化建议

#### 前端优化
```typescript
// 1. 组件懒加载
const HeavyComponent = defineAsyncComponent(() => import('./HeavyComponent.vue'))

// 2. 计算属性缓存
const filteredTasks = computed(() => {
  return tasks.value.filter(t => t.status === 'active')
})

// 3. 防抖和节流
import { debounce } from 'lodash-es'

const search = debounce((query: string) => {
  // 执行搜索
}, 300)
```

#### 后端优化
```typescript
// 1. 数据库查询优化
// 使用索引
CREATE INDEX idx_tasks_status ON tasks(status);

// 2. 缓存策略
const cached = await redis.get(`tasks:${projectId}`)
if (cached) return JSON.parse(cached)

// 3. 批量操作
await db.insert(tasks).values(tasksArray)
```

### F. 故障排查指南

#### AI调用失败
```
问题: AI接口返回错误
排查:
1. 检查API Key是否正确
2. 检查网络连接
3. 检查API额度
4. 查看浏览器控制台错误
5. 查看服务端日志
```

#### 数据丢失
```
问题: localStorage数据丢失
原因:
1. 浏览器清理缓存
2. 隐私模式
3. 存储空间不足

解决:
1. 定期导出备份
2. 使用数据库存储(Phase 2)
3. 启用自动同步
```

#### 性能问题
```
问题: 应用运行缓慢
排查:
1. 打开浏览器开发者工具 → Performance
2. 记录性能数据
3. 分析瓶颈
4. 优化相关代码

常见瓶颈:
- 大量DOM渲染 → 使用虚拟滚动
- 频繁API调用 → 添加缓存
- 大文件处理 → 使用Web Worker
```

---

## 🎉 结语

恭喜!您已经完成了PLAN-Tools V0.3的开发历程。这个版本实现了:

✅ **完整的Nuxt 3全栈架构**
✅ **强大的AI项目管理能力**
✅ **友好的用户体验**
✅ **安全的自托管部署**

接下来,您可以:

1. **开始使用**: 部署到服务器,日常使用
2. **持续优化**: 根据反馈改进功能
3. **Phase 2开发**: 添加用户系统和数据库
4. **商业化**: 探索商业模式

**记住**: 这只是开始。持续迭代,不断改进,才能做出更好的产品!

---

**文档维护**: 本文档随项目发展持续更新
**最后更新**: 2025-01-30
**当前版本**: V0.3
**下一版本**: V0.4 (Phase 2规划中)
